# Smart Market AI runtime profile.
# notebook: safer default for laptops and normal local checks.
# workstation: higher external-fetch concurrency for stronger local machines.
SMAI_PERFORMANCE_PROFILE=notebook

# LLM model selection is separate from the performance profile.
# Keep it unset unless a local LLM profile is intentionally selected.
# SMAI_LLM_PROFILE=notebook_dev
