# PROJECT_CONTEXT.md

## Overview

This file is the compact current-state summary for Smart Market AI.
Historical work entries belong in [Documents/99_Work_Log.md](Documents/99_Work_Log.md).

Last updated: 2026-07-11

Upward Signal (`上向き兆候`, internal compatibility key: Reversal Expectation) v3 is implemented
as a chart-shape-first ranking axis. It combines chart shape 30%, forecast upside 25%,
downside safety 20%, pullback/stability 10%, company/dividend quality 10%, and upward material
5%, then applies bounded falling-knife, weak-forecast, chase, and dividend-trap penalties.
The penalty-adjusted score uses a smooth midrange expansion so candidates no longer cluster near
55 while weak and dangerous candidates remain clearly separated. Fatal price-history quality
warnings remain unevaluated instead of turning ordinary metadata gaps into score caps. Ranking rows preserve the components,
shape label, trap warnings, dividend safety, yield-spike flag, and sustainability label across
ranking display, watchlist snapshots, ranking history, Cockpit context, and Decision Report
context. The dedicated chart uses chart-shape strength on x, upward potential on y, downside
safety as color, and data quality as point size; it selects another varying technical axis or
falls back to the upward-signal × downside-warning map rather than rendering a fixed-axis line.
A point-in-time backtest contract calculates
20/60/120-day returns, maximum drawdown, benchmark return, excess return, success/failure
summaries, and the four requested CSV/Markdown artifacts without exposing future prices to the
signal-row builder. Phase 34 live validation now covers 66 previously unused symbols and up to
ten years of bars with fixed symbol-disjoint tuning / validation / audit groups.

## Upward Signal / Forecast Improvement / Serious Analysis Strategy

Upward Signal is the public name for the backward-compatible Reversal Expectation axis. The next maturity work prioritizes ten-sprint point-in-time validation and existing advanced forecast model evaluation rather than adding models indiscriminately. `advanced_linear`, `advanced_tree_sklearn`, `advanced_gbdt_sklearn`, `advanced_quantile`, and forecast consensus will be compared by walk-forward horizon, market, asset type, regime, ranking contribution, disagreement, and confidence calibration.

Normal Ranking remains fast and LLM-independent. `本気分析モード（AI材料分析つき）` is an explicit opt-in plan for assessing only top candidates. Its initial role is badges, warnings, and summaries for material relevance, adverse news, and dividend traps. Ranking correction requires a prior performance gate and must remain bounded to top candidates.

Phase 33 evaluation and guarded improvement flow is implemented as a network-free backend service. It reruns every advanced adapter and the actual current consensus from bars available at each 20/60-day rolling origin, while adapter validation folds retain horizon-sized purge windows. It produces horizon, market, asset type, and regime metrics, latest predictions, and error cases. Candidate weights are built from earlier origins and become adoptable only when a later temporal holdout improves RMSE without reducing direction accuracy. Even adopted profiles require explicit `evaluated_consensus_prediction` use; Ranking, Forecast API/UI, and runtime model weights remain unchanged.

Phase 33 live evaluation completed on an explicitly fetched, isolated dataset of 23 symbols and 28,529 daily bars. `robust-linear-clip-v1` reduced advanced-linear RMSE by 74.2% at 20 days and 89.5% at 60 days; actual consensus RMSE fell by 24.0% and 48.4% respectively without lower direction accuracy. The clipping safety rule is adopted. Consensus-weight candidates failed the new minimum 1% holdout gate or worsened results, so runtime weights remain unchanged. Aggregate 20-day GBDT and quantile candidates remain shadow-only until subgroup stability is demonstrated.

Phase 34 evaluated 66 symbols not used by Phase 33 with fixed symbol-disjoint tuning (23), validation (22), and sealed audit (19) groups. A general 55% maximum one-day discontinuity gate excluded two split-adjustment anomalies. Bottoming and accumulation shapes now require a higher-low or volume-recovery confirmation and no longer create an unconfirmed 80-point shape by themselves. On the sealed audit, successful cases averaged 63.96 versus 51.37 for failures, but Top10 success was only 3/10, below the 7/10 target. Bounded consensus weights and calibration candidates failed temporal or cross-symbol adoption gates, so runtime forecast weights and predictions remain unchanged. Further threshold tuning stopped to avoid audit-set overfitting. See `reports/phase34_sprint_summary.md`.

Strategy references:

- [上向き兆候 戦略](Documents/32_Upward_Signal_Strategy.md)
- [LLMランキング融合 戦略](Documents/33_LLM_Ranking_Fusion_Strategy.md)
- [既存予測モデル改善 戦略](Documents/34_Forecast_Model_Improvement_Strategy.md)

Windows home-server operations now include AC-only power-policy setup, boot-time
SMAI/watcher scheduled tasks, five-minute Streamlit/TCP 8501 recovery monitoring,
and a fail-closed 24-hour SMAI service-restart manager. Scheduled startup uses a
resilient Windows process group so a console interrupt cannot stop the shared server.
Production startup disables Streamlit run-on-save reloads so source edits do not
discard active ranking sessions. Large live rankings keep one continuous workflow,
use bounded 25-symbol Yahoo fetch batches with per-symbol fallback, and keep completed
builds in a process-wide cache so reconnecting sessions can restore the
same-condition result. Same-day OHLCV, fundamentals, and advanced forecast outputs
are reused; fundamentals use four-way async concurrency and advanced forecasts use
up to two local worker threads.
Connected Streamlit sessions
publish one-minute heartbeats; background news/symbol refreshes publish busy markers;
file locks and unreadable state defer restart. A 30-second UI notice is followed by
a second safety check, and any new activity cancels restart. Maintenance restarts
Streamlit only; it does not reboot Windows. See
`docs/server_operation.md`.

## Project Summary

Smart Market AI is a Python-based investment support project that combines market-data ingestion, feature generation, screening, deterministic baseline forecasting, model-informed Investment Score, portfolio/risk checks, Streamlit UI, Decision Report context, and Research RAG evidence workflows.

The product direction is to help users compare symbols, inspect provider-backed data, review model signals, understand warnings, and make better-informed investment decisions without turning the app into automated trading.

## LLM Expansion Strategy

- SMAI will expand LLM usage from Copilot to the broader investment workflow.
- The LLM layer interprets price, forecast, ranking, RAG, news, disclosure, and Decision Report contexts.
- The first role is explanation, summarization, material extraction, contradiction detection, and report drafting.
- LLM outputs remain non-authoritative confirmation materials until validated.
- LLM-derived factors are considered for Forecast / Ranking / AI総合 / Investment Score integration only after historical validation, leakage checks, and baseline comparison.
- Gateway / provider failures must fall back to deterministic behavior, and normal checks remain network-free.
- LLM model selection is owned by `smai-ai-gateway`: SMAI parent sends `task_type`, `execution_mode`, `environment_profile`, and optional `profile` / `model`, while Gateway resolves `notebook_dev` / `notebook_standard` / `desktop_fast` / `desktop_analysis` / `desktop_heavy` to provider/model/timeout/token settings and returns response metadata for the UI. Notebook development defaults to `qwen3:1.7b`; the SMAIアシスタント model picker can switch to `qwen3:4b` / `qwen3:8b` / `qwen3:14b` / `qwen3:30b`.
- SMAI Assistant now treats valid Gateway answers as `response_source=llm` and reserves `response_source=deterministic_fallback` for Gateway/provider/model/timeout/schema/empty-answer failure paths. Assistant turns preserve `request_id`, `gateway_status`, `fallback_reason`, `latency_ms`, `provider`, `model`, `profile`, `timeout_sec`, `context_tokens_estimate`, `prompt_chars`, `response_chars`, `tool_execution_ms`, `llm_generation_ms`, `total_elapsed_ms`, conversation mode, and Gateway/provider diagnostic metadata. The UI keeps runtime metadata in a folded `技術情報を表示` block for analysis-style answers, but hides technical fallback metadata for normal chat / identity / capability turns. The header uses a centralized `AssistantRuntimeStatus` model for ready / checking / generating / research planned / research running / degraded / Gateway unavailable / provider unavailable / model missing states, keeps first display neutral as `LLM待機中`, and refreshes from pending events, cached diagnostics, or the latest answer response so success / fallback status stays aligned with the chat. New conversation clears stale runtime status and Gateway diagnostic cache.

Current focus is project maturity improvement rather than feature expansion. Functional spec issues and a manual UX review checklist have been introduced so confusing behavior, unclear role boundaries, and investment-advice-like wording can be reviewed before more advanced features are added.

Notification Platform Phase N1-N5-C foundation is implemented as an independent `smai-notification-gateway`, parent client/adapter, versioned SQLite user settings/history, notification center, catalog Producer, and opt-in scheduler. `data/user/notifications.sqlite` is created on first settings access; blank topic input preserves the stored topic, only the dedicated delete action clears it, and the UI never redisplays it in plaintext. HTTPS is allowed, HTTP is limited to loopback, equal quiet-hour endpoints are rejected, and severity is fixed-choice. Real ntfy delivery is reachable only from explicit opt-in paths; normal checks use fake clients/bindings/transports and remain network-free. N6 real favorite/news/market adapter integration remains pending. See `Documents/04_Detail_Design/04-10_Onepager_Notification_Platform.md`.

Notification content is specified as a future common `NotificationContent` layer rendered separately for in-app cards and compact ntfy messages. Planned presentation categories are Favorite, Market Trend, Investment News, SMAI Insight, and System. Cards must state what changed, why it matters, the SMAI confirmation material, the next check, and a safe navigation CTA. `AI recommendation` wording is prohibited in favor of confirmation candidates; favorite-since-added performance is a monitoring-period reference, not SMAI recommendation performance, and missing registration snapshots must not be inferred. Notification fatigue controls include daily aggregation, dedupe, cooldown, severity threshold, and quiet hours.

Notification Phase N4 is implemented as the safe receiving surface. Schema v5 preserves N3 settings and adds in-app history, sanitized delivery results, local users, per-category notification preferences, and a non-deletable `default` / `SMAIデフォルト` system user. `NotificationService` persists history before any optional delivery; only the explicit test notification is connected as a Producer. The responsive fixed top-right user area is a compact three-item entry for user settings, notification settings, and returning to user selection. User settings contains profile and icon controls only. The dedicated notification settings view combines notification categories, app/ntfy destination, severity, quiet hours, explicit test delivery, and one save/cancel action; the general Settings/Data page no longer duplicates this form. Profile images are direct selection targets, and every new browser session uses the profile gate instead of Trusted Device auto-selection. Legacy trusted-device tables remain migration-compatible but are no longer used by the UI. Automatic market/news/score Producers remain N5 scope.

Notification Phase N5-A through N5-C is implemented as a deterministic foundation. A typed catalog defines Favorite daily/move/news, Investment News, Sector Momentum, SMAI analysis/report, and System templates with channel, asset, schedule, severity, wording, layout, CTA, and sample data. `CatalogNotificationProducer` respects app/category settings and deterministic dedupe keys. The dedicated notification center hides the sidebar and provides summary counts, category/state/period/sort filters, scrolling image cards, details, read/archive, safe navigation CTA, and a debug-only catalog/manual generator. Opt-in schedule settings, registered daily jobs, atomic run claims, sanitized run logs, a polling runner, and `scripts/run_notification_scheduler.bat` are available. Scheduled Producers currently use deterministic catalog sample/cached-shaped data; live favorite/news/market cache adapters and event-driven Research/report hookups remain N6 integration work.

User profile icons are now manifest-backed local assets rather than emoji identities. Only enabled, existing entries are selectable; the database stores the icon ID only. The initial built-in reuses the official SMAI PWA icon, while missing selections fall back through the default asset, existing local mascot thumbnail, and finally a CSS silhouette. User selection uses large profile cards and the fixed top-right area uses a 32-48px circular rendition with responsive name/ID reduction. New mascot image generation, external URLs, and custom uploads remain deferred.

LAN内PWA風アクセスの初期slice is implemented. `scripts/run_lan_server.bat` starts the
existing full Streamlit app on `0.0.0.0:8501` only when explicitly selected; normal startup,
EXE behavior, Assistant, Gateway, and Agent Workflow remain unchanged. Streamlit static serving
serves the official SMAI home-screen icons and web manifest from `ui/static/pwa`,
best-effort iOS metadata is injected without modifying Streamlit internals, and mobile-only CSS
adds touch targets, horizontal table scrolling, narrow-screen stacking, and a bounded floating
assistant. `docs/LAN_PWA_ACCESS_GUIDE.md` limits use to a trusted same-LAN network and documents
IP, Private-profile Firewall, router DHCP reservation, home-screen addition, and non-PWA limits.
Desktop server operations MVP adds logged, duplicate-safe scheduled startup, local dependency
status checks, guarded SMAI-only shutdown, and idempotent logon-task registration/removal.
Symbol-universe bulk import remains separate from LAN server startup. A maintenance if-due task
runs 10 minutes after logon, but calls the heavy existing import only when the last success is at
least 7 days old; failures use a 24-hour cooldown. Machine-local atomic state/lock files and
maintenance logs record attempts, success, exit code, and duplicate/stale-lock skips.
Symbol-universe maintenance reports are grouped by local execution time under
`reports/YYYY-MM-DD_HHMM/`; the heavy import writes directly to that run folder.
Streamlit CSV exports now use UTF-8 BOM bytes, `.csv` filenames, `text/csv`, and fragment-scoped
download buttons so LAN latency does not race a full-app rerun against Streamlit's in-memory media
URL. Empty Rebalance/score datasets do not render header-only download buttons.

External-access/PWA stability now extends beyond profile icons. Shared logo, mascot, Assistant,
loading, and title artwork uses cacheable `/app/static/assets/` WebP variants instead of bitmap
base64 in rerun HTML, while source artwork remains under `ui/assets/`. Assistant answer exports no
longer use HTML `data:` links; the latest answer exposes in-app preview/copy plus fragment-scoped
Markdown download, and Decision Report ZIP remains download-only. Settings includes an opt-in
external-connection diagnostic for localhost/LAN/Tailscale classification, Streamlit connection
settings, optimized-asset totals, and session-state size estimates; snapshots log metadata and
sizes only. Deterministic delivery/asset reports live under `logs/server_ops/`; live Tailscale and
iPhone/iPad Safari/PWA verification remains a separate manual six-environment check.

Mobile/PWA session recovery uses a lightweight, network-free per-client snapshot. A safe random
`client` query parameter identifies the browser/PWA, and its allowlisted state is stored under
`data/user_state/clients/<client_id>.json`. The last valid user, primary page, Cockpit symbol,
four main Ranking conditions, and explicit MarketData provider selections are restored
automatically for 30 minutes. Older snapshots and snapshots referencing deleted users are removed
and startup returns to the user-selection gate. Explicit profile/page URL query parameters still
take priority. Corrupt/oversized JSON and read/write failures safely fall back without breaking
startup. Restoration never starts price fetch, Ranking creation, Research/News retrieval, or
other heavy work. The user menu can release the current device session. The Streamlit 1.38
configuration uses only supported server options; client snapshot recovery,
not Streamlit disconnected-session retention, is the recovery mechanism. iPhone/iPad Safari/PWA
and Tailscale behavior still require the documented manual device check.

Phase 31-SDB Screening DB Reliability is implemented as a backward-compatible DB quality slice. The symbol metadata schema now accepts separate official-industry / SMAI-theme fields, SBI/NISA confirmation states, ETF asset-class/AUM/average-volume fields, and per-metric source/as-of/quality metadata without requiring new columns in existing CSVs. Yahoo metadata refresh can fill missing values only and preserves existing canonical values; unavailable values remain null. `symbol_universe_quality_report.json` provides network-free total, region, product, Japan-stock, US-stock, and ETF coverage. The 2026-06-22 deterministic screening backfill fills JPX official `tse_33_industry` / `topix_17` for 3,746 Japan-stock rows and one-to-one mappable `sector_gics` for 3,662 US-stock rows, while keeping ambiguous consumer GICS splits blank. Ranking/Cockpit UI now separates `業種・セクター` from SMAI `投資テーマ`; theme filtering uses `theme` / `smai_theme_tags`, and official-sector filtering uses `sector` / GICS / JPX fields. Ranking UI now calls the `risk_band` filter `値動きリスク` instead of presenting the band as an exact beta value. Phase 31-SDB operational maintenance on 2026-06-26 added deterministic provenance normalization via `tools/normalize_symbol_universe_quality.py`, a reviewed patch workflow via `tools/export_symbol_universe_metadata_gaps.py` and `tools/apply_symbol_universe_metadata_patch.py`, and chunked weak-Asia refresh auditing that records `no_update_symbols`, `unchanged_update_symbols`, and batch `no_update_symbols.csv` outputs. Live backfill remains explicit opt-in and Execution/Broker scope remains deferred.

Cockpit market-data fetch, Research update, and Investment Radar news refresh now share a blocking SMAI workflow loading surface with current step, progress, and cache-only `市場トピック`. Ranking creation uses the same visual as an inline card below its progress bar so the existing screen is not visually blocked; true interactive background ranking remains follow-up because ranking creation is still synchronous. Investment Radar headlines now show four unique items at a time in a two-by-two board, rotate every six seconds, pause on hover, support manual page dots, and stop motion under reduced-motion preferences.

Performance profile first slices are implemented with `SMAI_PERFORMANCE_PROFILE=notebook|workstation`. The profile controls Research RAG external fetch concurrency, provider request timeout, global timeout, HTTP retry/backoff for EDINET / TDnet / company IR / Google News RSS, provider-to-profile source mapping, and source-level latest fetch summaries through `DefaultExternalResearchAdapter`. Global timeout returns available partial results and records unfinished providers as `timeout` in `ExternalResearchFetchResult.provider_statuses`; Cockpit shows the provider status chips/warnings, and Streamlit `設定 / データ情報` shows the active profile plus the latest source summary using stable table rendering. MarketData now defaults to the configured provider, and the product default is Yahoo live data across API / Cockpit / Ranking / related UI screens. `per_source_workers` has a Research source-limiter entry point, but adapter-internal URL/page parallelism is still deferred. News dashboard refresh, Symbol DB background refresh, Yahoo/yfinance timeout integration, and processing worker settings remain follow-up scope.

Current UI maturity work includes Ranking visualization polish: existing ranking results are being presented as screening / comparison candidates with summary cards, top-candidate cards, score and confidence charts, selected-candidate breakdown, and a detailed table, without changing backend ranking or scoring logic.

Phase 32-A Myウォッチリスト MVP is implemented as a user-local favorite-symbol flow. Ranking, Cockpit, and 投資レーダー can toggle `☆ お気に入り` / `★ お気に入り中`; favorites persist to ignored local JSON at `data/user/favorites.json`; the side menu places `Myウォッチリスト` between 投資レーダー and SMAIアシスタント. The watchlist page shows local metadata when available and keeps missing live-derived fields as `未取得`; AI調査 / レポート card actions navigate to Cockpit with explicit follow-up guidance rather than auto-running external fetches or report generation.

Phase U1 Local User Profiles is implemented. Custom local profiles can be created with an internal random ID and local icon; favorites, Decision Trail fields, refresh metadata, and watchlist snapshots resolve through the active profile under `data/user/profiles/<user_id>/`. The non-editable `default` system profile stores favorites and snapshots only in Streamlit session state and never creates a profile file. Default notifications are hidden and rejected by settings, history, Producer, service, scheduler selection, test-send, and gateway boundaries. A one-time non-destructive migration copies legacy shared favorites/snapshots to the existing `local_user` profile when available. These profiles are local selection boundaries, not authentication.

Phase U1-Verify is complete. The broken Windows Store-backed virtual environment was replaced with a Python 3.11.9 workspace-local base and a rebuilt `venv_SMAI`; the previous environment remains as ignored `venv_SMAI_broken_20260630`. U1/favorite/snapshot/notification tests pass (71 tests), targeted Ruff and Mypy pass, and the project Black helper passes all 352 Python files. An opt-in Playwright smoke verifies custom-user creation, immediate app start, default notification-menu removal, page overflow, and add-user cancel behavior at 1366x768 and 375x812. N6 remains gated on the active-user Favorite Store and must continue excluding system users.

Phase 32-B first cross-link slice is implemented. 投資レーダー can use favorites as the Watchlist source (`Myウォッチリスト`, `My + 手入力`, `手入力のみ`), and combined mode de-duplicates symbols without mutating `favorites.json`. Myウォッチリスト now has card/table display switching, summary chips, a local last-checked update button, and memo/tags display. The update path remains deterministic/local-first and does not change Ranking scores, Research logic, Decision Report generation, or provider fetch behavior.

Phase 32-C Favorites-aware Refresh MVP is implemented. The Myウォッチリスト page-title `watchlist` mascot key no longer raises `KeyError`, and unknown title mascot keys fall back safely. Favorites now support refresh metadata and a deterministic status helper for `未確認` / `古い` / `要確認` / `最新` / `前回失敗`; Myウォッチリスト shows refresh status, next action, last checked time, and update summaries. `ウォッチリストを更新` only updates local refresh metadata for prioritized non-fresh favorites up to the selected maximum count; it does not start background refresh, external fetch, AI調査, Decision Report generation, or ranking-score changes.

Phase 32-C2 Myウォッチリスト UI Polish is implemented. The shared favorite button now visually distinguishes registered symbols with a restrained gold/amber `★ お気に入り中` treatment while keeping unregistered `☆ お気に入り` buttons navy/blue-gray. Myウォッチリスト cards are grouped into header, status/refresh badges, metric cards, confirmation info, and action buttons; missing price / score / check fields remain safe as `未取得` / `未確認`. The `watchlist` title mascot points to a replaceable dedicated asset filename and falls back to the Investment Radar art if the image is not yet placed.

Phase 32-D / 32-E Myウォッチリスト completion is implemented. Favorites now carry backward-compatible Decision Trail fields for Watch理由, 判断状態, 判断メモ, 次の確認, 最終メモ更新, and future trail entries; Myウォッチリスト cards/tables show these fields, expose one-symbol-at-a-time editing, and pass watchlist context plus explicit next-action hints when opening Cockpit / AI調査 / レポート. The card header now prioritizes company name over symbol. My Radar adds display-only priority, summary categories, candidate lists, and filter/sort controls without changing Ranking / AI総合 / Research Score, provider fetch behavior, or the saved favorite order. Investment Radar related-symbol rows for `本文に出た銘柄` and `SMAI推測候補` now render as compact horizontal chips with the existing favorite toggle aligned to the matching symbol.

Watchlist Groups MVP is implemented as a presentation layer separate from favorites. Custom
profiles persist versioned group definitions and one-symbol/one-group placements at
`data/user/profiles/<user_id>/watchlist_groups.json` with atomic replacement; the default
system user remains session-only. Myウォッチリスト defaults to grouped sections using the
existing full-information favorite cards, allows each section to be collapsed in session, and
computes a final slate `未分類` section while retaining the existing `すべて` card/table view.
A large dedicated editor keeps group/placement changes in a session draft until
`保存して閉じる`; it handles group add/edit/delete, preset tones, and touch-safe destination
controls without placing them in normal cards. The editor uses the project-owned
`ui/components/watchlist_sortable` component, derived from the earlier Apache-2.0 sortable
approach, as a multi-container chip board: symbol/name chips can move
between groups/`未分類` and reorder within a group, while validated results update only the
session draft until save. Stable group IDs and normalized symbol IDs keep labels separate from
D&D identity. Cross-container previews always calculate from the latest local state, cancellation
restores the drag-start snapshot, and only a complete unique payload is accepted. Actual drops
use a fixed iframe key plus monotonic client sequence/server acknowledgment; old rerun props cannot
overwrite newer local moves, and acknowledged server state syncs without per-drop iframe remount.
Pointer-under-chip/container collision detection and a persistent tail drop
lane keep multi-group crossings and wrapped multi-row groups targetable. Each custom group container owns touch-safe
`↑` / `↓` / `編集` controls in its D&D header; `未分類` has none. The edit action opens that
group's settings inline only on demand, with no shared selector toolbar or lower editing stack.
The normal screen exposes only the top-level editor action; each tone-colored group header
is itself the expand/collapse control, with no separate create, close, or per-group edit buttons.
Each normal-screen group is wrapped in a tone-colored panel covering its header, description,
cards, and actions. Explicit tone gradients also apply to editor drop zones. Touch chips
suppress page swipe while dragging, while surrounding board space remains scrollable. Favorite
removal keeps placement for possible restoration when the symbol is favorited again.

Phase 32-E2 compact polish is implemented. My Radar uses a five-count summary with reasons inside an expander; update options are collapsed while the two explicit actions remain side by side; filter/sort and card/table controls stay display-only. Cards show a compact missing-note state until Decision Trail content exists, then reveal the full decision fields. Favorites schema, Radar priority, local-first refresh, and explicit AI調査 / Decision Report behavior are unchanged.

Phase 32-F daily watchlist UX is implemented. Display filters use stable internal values with count-aware chip labels; cards add restrained state accents and 1-day / 5-day / 1-month movement markers from optional local change fields, with safe missing/NaN handling and a compact data-update-needed message. On the first watchlist render per session, at most three missing/failed/stale/attention symbols outside a six-hour TTL are registered with the existing symbol DB daemon target queue. The queue remains local-cache-first and non-blocking; live price/provider fetch, AI調査, external news refresh, and Decision Report generation are not auto-run.

Phase 32-G Watchlist Snapshot Cache is implemented. User-local `data/user/watchlist_snapshots.json` stores normalized symbol snapshots independently from favorites, safely falls back on missing/broken JSON, and preserves previous values on failed updates. Watchlist cards/tables/Radar/filter/sort prefer snapshot price, 1/5/20-business-day changes, trend, timestamps, and copied existing score fields, with field-level fallback to Ranking/Cockpit session and local symbol rows. The explicit update button alone may fetch bounded OHLCV when provider opt-in permits; it does not auto-run Forecast, scoring, Research, News, or Decision Report.

Phase 32-H final watchlist UX is implemented. Normal view keeps only the Radar count strip and removes Radar-reason/update-options/empty-note expanders; six count-aware filters, newest-added default sorting, three-column desktop cards, stronger restrained trend backgrounds, natural missing-data copy, and a subdued remove action reduce visual weight. The first watchlist visit per session may auto-refresh up to three eligible snapshots with the shared loading mascot and six-hour TTL; worker/provider gates and prior-data preservation remain in force, with no automatic Research, News, or Decision Report.
The final three-pass runtime polish also removes the remaining empty `判断メモ` row, fixes snapshot `price` display precedence, and lets bounded auto/manual refresh reuse the existing Cockpit preview path so missing AI総合 / 上昇気配 / 下振れ警戒 values are filled together with OHLCV when provider opt-in permits. The dedicated `smai-title-watchlist.webp` now shows the SMAI mascot playing with favorite stars instead of using the Investment Radar fallback.
Watchlist card actions now reuse the Ranking `銘柄データ` dialog for in-place detail and AI Research. The dialog expands to a wide desktop layout and AI Research loading is contained inline inside it. Card-level AI調査 / レポート navigation buttons are removed; `銘柄を詳しく見る` opens the modal, `Cockpit画面で確認` is the clearly styled page transition, and remove remains visually secondary.

Ranking visualization maturity now uses condition-aware chart profiles, purpose-specific focus summaries, purpose-specific table leading columns, and row-level sorting reasons / checkpoints. The main scatter chart changes by ranking purpose, uses `上昇気配 x 下降警戒` for multi-factor / upside views, falls back to available existing columns, and treats Evaluation Confidence as a data-reliability / coverage support metric except for data-confidence-focused rankings.

Ranking detailed table maturity now keeps beginner comparison columns visible by default: `現在株価（円）`, `総合スコア`, `配当利回り`, `PER`, `PBR`, and `ROE`. `現在株価（円）` is display-only JPY-unified context; USD source prices are converted with the latest fetched USDJPY rate when available, while non-convertible non-JPY prices stay `N/A` instead of being shown as yen. The Ranking top UI uses `評価方針` as the main path for SMAI composite profiles (`AI総合`, `上昇気配重視`, `モメンタム・トレンド`, `成長クオリティ`, `割安クオリティ`, `高配当の持続性`, `低ボラ・安定`, `安定成長`, `小型・成長探索`, `NISA長期適合`, `データ信頼度優先`, `ETF低コスト・コア`, `ETFインカム・分散`). `AI総合` now displays a tuned grouped profile of `基礎評価30%` / `予測・上昇気配30%` / `リスク・下振れ警戒25%` / `データ信頼度10%` / `Research確認材料5%`, and each composite policy carries short summary / suited-for / main-focus / caution metadata for the condition card. Single-metric sorting is not a top dropdown; users sort displayed candidates locally by clicking detailed-table columns such as `現在株価（円）`, `総合スコア`, `配当利回り`, `PER`, `PBR`, `ROE`, `時価総額`, `出来高`, `ボラティリティ`, `リスク`, `データ信頼度`, `基礎評価`, `上昇気配`, and `下降警戒`. Missing values remain `N/A` and sort to the end. Ranking refresh is tied to the explicit `ランキング作成` action, while evaluation-policy changes and table sort/search/filter operations reuse fetched data and do not fetch external data.
The Ranking top flow now includes a compact `評価方針・条件・信頼度の読み方` guide before data fetch. It distinguishes evaluation policies from pre-fetch detail filters, and explains that `条件適合度` / `DB信頼度`, NISA, dividend / distribution yield, and ETF cost / income criteria are comparison / coverage checks rather than investment attractiveness, safety, or product-suitability judgments.

The current `data/marketdata/symbol_universe.csv` master has 11,071 rows. Yahoo remains the canonical live-metadata source for 9,197 rows, while additional SBI-derived overseas seeds cover Hong Kong, Vietnam, Korea, Thailand, Indonesia, Malaysia, and Singapore candidates until reviewed/backfilled. Remaining provider-missing fields stay blank and display as `N/A`; reviewed manual patches are now the preferred operational path for trusted low-coverage fixes before adding more providers. Dividend yield, PER, PBR, and ROE are guarded at write/display/ranking boundaries so over-scaled or out-of-range values are cleared before they can affect ranking. Web pages remain sanity-check references rather than exact parity requirements because metric definitions and update timing can differ.

Ranking logic now uses `upside_signal_score` and `downside_signal_score` as the primary forecast-derived ranking/cockpit indicators. Direction signal v2 scores forecast edge, model-by-model direction edge, recent price momentum, and simple trend confirmation after volatility adjustment, so future forecast models contribute by direction and magnitude rather than only up/down counts. Agreement / model spread is treated as a confidence adjustment with a 0.70 floor, not as an additive bonus. Ranking / preview signals use fetched feature-history bars rather than only the selected display period, so short ranking periods do not force every symbol into neutral 50 / 50 fallback. `forecast_agreement_score` remains for compatibility and as a supplementary model-consistency metric; older direction net/label fields may remain in backend contracts for compatibility but are not main ranking/cockpit UI indicators.

Phase 23 Advanced Forecast now has an adapter registry plus `advanced_linear`, `advanced_tree_sklearn`, `advanced_gbdt_sklearn`, `advanced_quantile`, and an advanced forecast consensus layer. `scikit-learn==1.5.2` is a runtime dependency for the tree / boosting adapters; `advanced_tree_sklearn` defaults to deterministic `ExtraTreesRegressor`, and `advanced_gbdt_sklearn` uses deterministic `HistGradientBoostingRegressor` with fixed `random_state` and internal early stopping disabled. For Cockpit/API, all four advanced adapters support 1-60 day horizons and Cockpit passes the same period-derived forecast horizon to baseline and advanced models so chart lines, cards, and detail rows compare the same forecast date. The default Cockpit/Ranking horizon is derived from the displayed acquisition period at roughly one twelfth of the window, capped at 60 days. Cockpit charts now lead with actual price, the default `AI予測インサイト` consensus line, and its lower-to-upper prediction range band; advanced model lines and simple forecast lines are added with two grouped chart checkboxes (`高度予測モデル`, `単純予測モデル`) for detail confirmation instead of crowding the initial chart. These checkbox bindings live inside the Altair chart, so they update in the browser without rerunning Streamlit or recalculating forecasts. The chart uses a fixed-color in-chart legend that follows the checked series set and can dim individual displayed series while keeping colors stable. The chart keeps a full `価格チャート` view plus a compact right-side `予測スコープ` chart for the latest actual days and forecast area, small point markers while keeping the actual-price line thinner, and a `表示通貨` control that offers only JPY / USD, defaults to JPY for JPY or unknown source currency and USD for USD source currency, and shows only a short `＄円相場` USDJPY value beside the radio buttons. Naive / moving-average / momentum simple forecasts are kept as backend baseline / fallback / detail context but are no longer part of the default Cockpit chart or main model-card display. Cockpit `データを取得` shows a progress bar for input confirmation, price / forecast fetch, score / chart shaping, and display update. Cockpit now shows a conclusion-focused `AI予測インサイト` card before individual advanced model details; the card uses `中心予測` as the main display name for the advanced-model consensus, then shows downside / upside cases with tooltips, forecast price, forecast range, confidence, model agreement, forecast dispersion, reasons, cautions, and forecast horizon in the primary view. Low-confidence or judgement-hold states use an amber accent. Individual advanced model cards are always visible below the chart, while RMSE, error improvement, historical direction accuracy, relatively stable model details, and simple forecast baseline comparisons are folded under `高度予測モデルの詳細を見る`, `検証指標を見る`, and `単純予測との比較を見る`. Forecast chart / card helper text includes beginner-friendly formulas for consensus weighting, baseline models, linear return prediction, tree / boosting split-based checks, and quantile range construction. Ranking rows now retain one common-horizon advanced forecast consensus return (`advanced_forecast_predicted_return`), horizon days, score, and confidence context across table/detail/CSV review when enough local history exists. Ranking detail/breakdown labels now call this `AI予測インサイト`, while table/CSV compatibility columns keep `高度予測` naming. Ranking derives `advanced_forecast_upside_score`, `advanced_forecast_downside_score`, and `advanced_forecast_quality_score` from consensus return, lower range, confidence, agreement, direction accuracy, RMSE improvement, and validation sample context; Ranking rows blend these into `upside_signal_score` / `downside_signal_score` at 25% when present, and `AI総合` places the derived advanced scores inside the tuned `予測・上昇気配30%` and `リスク・下振れ警戒25%` groups. The Ranking UI now shows a `今回のランキング条件` card with the selected evaluation policy, short summary, suited-for text, main-focus chips, common forecast horizon, grouped weight profile, low-is-better caution for downside metrics, and a folded beginner term table for `AI総合`, `上昇気配`, `下降警戒`, `AI予測インサイト`, advanced upside/downside/quality, and horizon. Low-confidence or missing data is pulled toward neutral 50. Ranking policy weights are tuned for AI総合, small-growth, ETF income/core, and selected Research 5% confirmation material, while LLM Factor / interpretation outputs remain outside ranking scores. Advanced forecast outputs remain decision-support context, not investment advice.

Phase 23 closeout now also aligns Ranking and Decision Report wording around `AI予測インサイト`: Ranking sort reasons and checkpoints explain that advanced forecast is blended into `上昇気配` / `下降警戒` up to 25%, selected-candidate breakdown and score detail rows place `AI予測インサイト` next to direction signals, and Cockpit / Ranking Decision Report sections preserve the same advanced-forecast context in score decomposition, candidate memo, score detail, distribution, factor leaders, and group checkpoints.

Phase 24 Assistant now has a stable Streamlit UI foundation: the fixed floating `SMAI Copilot` mascot remains section-local quick guidance for Cockpit / Ranking / News / Rebalance / Settings, while the side-menu `SMAIアシスタント` page is the dedicated chat workspace. Floating guidance uses the canonical face-up mascot thumbnail, contextual question chips, no query-parameter navigation, in-browser chip switching, and answers that explain visible reasons, cautions, and next confirmation points without buy / sell / hold advice.

The Gateway boundary slice lives in `backend/assistant`: `AssistantContextBundle`, `AssistantGatewayRequest`, and `AssistantGatewayResponse` convert SMAI context into compact Gateway-safe payloads while excluding provider raw fields, debug logs, full external source bodies, and source metadata. `AssistantGatewayClient`, network-free `MockAssistantGatewayClient`, opt-in `HttpAssistantGatewayClient`, settings-based service factory, and `GatewayBackedAssistantService` prefer valid Gateway responses and fall back to `TemplateAssistantService` on Gateway errors, timeout, schema validation failure, invalid JSON, empty answers, or missing context. The generic Assistant service keeps `assistant.gateway.enabled=false` as the network-free default, while the dedicated `SMAIアシスタント` workspace uses the local `smai-ai-gateway` by default and falls back deterministically when needed.

Assistant requests now carry chat fields such as `conversation_id`, `message_history`, `active_context_id`, and `referenced_context_ids`. The `SMAIアシスタント` workspace has context presets for Cockpit, Ranking, News, AI材料分析, and Rebalance, guided question buttons, 240-character limited free text, session-local conversation history, a chat-width `新しい会話` action below the header, one-at-a-time current pending step display, inline chat-placeholder replacement for normal submits, grouped pseudo-streaming for the newest answer, compact response actions, and the shared `AssistantContextBundle` path. It remains separate from the bottom-right floating helper: floating Copilot is section-local quick guidance, while the chat workspace carries lightweight conversation history for cross-screen consultation.

The `smai-ai-gateway/` foundation scaffold is now created inside this repo as a future independent repo / Git submodule candidate: a generic FastAPI AI Gateway with `/health`, `/health/ready`, `/models`, `/api/v1/chat`, `/api/v1/summarize`, `/api/v1/context-answer`, `/api/v1/llm-factor/generate`, Pydantic schemas, Ollama client boundary, model routing, services, tests, setup docs, prompt policy, provider error detail, opt-in live-smoke test path, and `Project_Specification.md` as the current-specification entry point. `context-answer` lets the LLM produce the answer while Gateway derives or validates supporting fields such as `materials`, `cautions`, `next_checkpoints`, and `referenced_sections` from the supplied context. `llm-factor/generate` accepts one-symbol compact context and returns `llm_factor.v1` structured JSON for qualitative material extraction. It stays generic while keeping SMAI as the primary current client; concrete other-project requirements are not part of the current Gateway specification. Existing SMAI RAG code is not moved, and no import coupling to SMAI core is added.

The dedicated workspace is now labeled `SMAIアシスタント` in the side menu, and its header uses the transparent `SMAIナビ` chat icon from `ui/assets/mascot/smai-navi-chat-cutout.png`. Phase 25 has its first parent-side live LLM integration slice: the `SMAIアシスタント` workspace no longer exposes an LLM ON/OFF panel and instead uses the `smai-ai-gateway` connection by default for submitted chat questions, while still falling back to deterministic answers on Gateway failure / timeout / schema failure. Gateway `/api/v1/context-answer` asks the LLM for structured JSON fields (`answer`, `materials`, `cautions`, `next_checkpoints`, `confidence`) and adopts them only after schema validation; malformed LLM payloads, `????`, replacement characters, or mojibake markers fall back to safe context-derived lists. The local Ollama runtime now has `qwen3:1.7b` installed as the notebook default model; Gateway still sends `think: false` where supported to avoid exposing thinking output. Phase 26 has its first context-aware / agentic Assistant slice: the initial workspace now starts with header, chat-width `新しい会話` action, referenced materials, six intent cards (`app_help`, `stock_summary`, `forecast_risk_compare`, `news_materials`, `decision_report_draft`, `free_chat`), and the input bar without a large greeting card; the first SMAIナビ utterance appears only after the user submits text or selects a card. Free text is routed through a rule-based Intent Router, and a read-only `Assistant Tool Layer` can summarize current context, resolve common symbols, inspect available price / forecast / news / Research Evidence context, build a Decision Report draft context, and prepare Markdown memo output without changing scores, rankings, forecast values, settings, or source data. Phase 26A has its Command Center / Research Mode first slices: `ConversationModeDecision` separates `normal_chat`, `soft_research_suggestion`, and `research_plan`; clear research questions such as `トヨタはこれから上がるかな？` produce a chat-thread Tool Plan card with natural SMAIナビ wording, `銘柄名（コード）` target display when available, user-facing tool labels, and `取得して分析する` / cached-only / cancel actions before any external fetch or LLM answer generation. Approved plans show a chat-thread progress bubble with checked completed items and `確認中` current items. `AssistantResearchContextBundle` now aggregates read-only Tool results into confirmed materials, missing materials, cautions, and next checkpoints for LLM answer generation, final answer sections, executed checks, Markdown memo context, and Decision Report draft context. Research Mode answers now keep `decision_report_context` / `decision_report_markdown` on the assistant turn, and `Decision Reportに追加` creates a session-local `pending_decision_report_draft` preview from the exact turn. Cached-only answers explicitly avoid external fetch, mark planned external tools as missing, and list不足材料 while still producing a draft. `decision_report_request` can reuse the latest Research Mode draft when the user asks to turn the current content into a Decision Report; cancel answers end naturally without fetching. Assistant responses now render conversation-first: SMAIナビ natural-language lead-in appears before structured intent-specific sections, `実行した確認` and LLM / fallback metadata are visually separated below the answer, and the initial six cards are not repeated after the first turn. The pending SMAIナビ bubble shows only the current step, rotates through intent-specific work such as symbol confirmation, price / forecast material checks, news material sorting, and LLM answer generation, and does not display the full planned step list. Normal submits render pending and final answer through the same chat placeholder to avoid an extra post-submit rerun, and pending / assistant cards reserve minimum height to reduce vertical jump. The newest assistant turn uses UI-side grouped pseudo-streaming so the natural-language answer appears by sentence or chunk roughly every 0.16 seconds before support sections and actions; Gateway / LLM answers are rendered as the primary answer without an added fixed lead sentence, SMAI read-only Tool Layer results are passed as checked materials, `free_chat` suppresses fixed structured cards and execution chips, and `app_help` uses concise purpose guidance instead of result cards. The dedicated workspace now uses a centered single-column chat layout: user bubbles are right-aligned, SMAI responses are left-aligned, structured cards stay inside the assistant response, compact action links sit directly below each answer, the `新しい会話` action aligns with the header lane, and the input bar matches the chat-column width. Parent SMAI also has an opt-in `tests/test_assistant_gateway_live_smoke.py` path guarded by `SMAI_ASSISTANT_GATEWAY_LIVE_SMOKE=1`; actual Ollama / Gateway smoke execution remains explicit and outside normal checks.

Phase 30-C2 connects `update_research` to the SMAIアシスタント confirmable action flow. The action is still never executed without the explicit confirmation panel; after confirmation it calls the same session-local external Research fetch path used by AI調査, records only safe `AssistantActionResult` summary fields and session-local audit metadata, and displays success / partial_success / failed result cards. Raw provider responses, source body text, debug details, Ranking score, Forecast, Investment Score, AI総合, Research Score, broker, and execution behavior remain unchanged. Normal tests use injected fake fetchers and remain network-free.
Phase 31-A Product Copy / UI Text Polish is implemented as a UI maturity slice. Priority 1 normal-display copy now favors `確認メモ`, `根拠資料`, `詳しく確認したい候補`, and `スコア・予測・AI総合は変更しません` wording in Assistant confirmation/result cards, Ranking candidate guidance, and Decision Report support notes. Internal terms such as provider details and raw evidence wording remain out of normal cards or inside technical/detail contexts; no scoring, forecast, Research fetch, LLM, broker, or layout behavior changed.

Phase 24A `SMAI LLM Factor` has its deterministic SMAI-side foundation, cache / reproducibility, Ranking reference display, backtest first slice, and broader validation expansion: `backend/llm_factor` defines source-bound Pydantic contracts for `LLMFactorResult`, bullish / bearish factors, evidence sources, model name, prompt version, source hash, cache metadata, 0-100 material scores, Ranking reference DTOs, deterministic fake / cached services, deterministic backtest evaluator, broader historical fixture pack, extended classification / return / risk / baseline-comparison metrics, segment metrics, validation warnings, and JSON / Markdown validation report export. Phase 27-A / 27-B adds the opt-in live Gateway path for one-symbol Cockpit `AI材料分析`: compact context builder, dedicated HTTP adapter, `llm_factor.v1` validation, live cache key expansion, standardized fallback reasons, deterministic fallback, live smoke config/docs, Playwright panel smoke, and UI metadata for disabled / fallback / live provider / model / profile / missing fields. Ranking now keeps the normal detail table compact and shows display-only AI material columns (`ニュース材料`, `材料件数`, `材料信頼度`, `材料の新しさ`) only behind `詳細列を表示する`; cache hits are reused, cache misses use deterministic fake values, these columns are non-sortable reference information, and Ranking score / rank / Forecast / Investment Score remain unchanged. Validation remains explicitly separate from Assistant / Copilot explanation behavior and from any Ranking / Forecast / Investment Score weighting.

Phase 28-A Cockpit LLM Interpretation MVP is implemented in `backend/interpretation` and the Cockpit UI. SMAI compresses visible Cockpit price / Forecast / AI予測インサイト / Investment Score / Research Evidence / `AI材料分析` context into a redacted `AssistantContextBundle`, calls `smai-ai-gateway` `/api/v1/context-answer` with `task_type=cockpit_interpretation` only when `llm_interpretation.cockpit.enabled=true`, validates policy and referenced section safety, caches by context hash / prompt / schema / model / profile, and falls back to deterministic `disabled` / `fallback` / `validation_error` memos. The `AI解釈メモ` panel is reference-only and does not change Ranking, Forecast, AI総合, Investment Score, Research Score, Assistant tool execution, Decision Report contents, portfolio, or execution behavior. Normal checks remain network-free with mock Gateway clients and a Playwright panel smoke.

Phase 29-A Cockpit Information Architecture Cleanup is implemented in the Streamlit Cockpit UI. After price / forecast, the main reading flow is now `03 AI解釈メモ` -> `04 スコア・リスクの内訳` -> `05 根拠資料` -> `06 確認レポート` -> `07 詳細データ`. `AI材料分析` is kept as a reference-only material memo under evidence, while provider / model / cache / runtime metadata is moved into closed detail expanders. Cockpit report wording is softened to `確認レポート` in the normal UI. Forecast, Ranking, Investment Score, LLM Factor generation, and Research external fetch logic are unchanged.

Phase 29-B Cockpit Pre-Fetch Header / Search Filter UI is implemented in the Streamlit Cockpit UI. The data-fetch area now starts with `銘柄を探す` for provider, search, symbol selection, symbol detail, and company name, followed by `絞り込み条件` chips that summarize region / NISA / product / active conditions / candidate count. Detailed filters stay closed inside the `絞り込み条件を変更` expander, and the clear action appears only inside that expander when a condition is active. Date range, `データを取得`, post-fetch content, Ranking, Forecast, Investment Score, and scoring logic are unchanged.

Roadmap order has been clarified for post-LLM expansion: Phase 25-30 now cover opt-in live LLM Gateway smoke, context-aware / agentic SMAI Assistant, live LLM Factor generation, LLM interpretation across Cockpit / Ranking / Radar / News / Research Summary / Decision Report, LLM-assisted Decision Report drafting, and validation-first gradual model integration. Phase 28-A Cockpit LLM interpretation is implemented as a reference-only `AI解釈メモ`; 28-B+ remains screen expansion work. Early LLM phases are explanation / summarization / material extraction / report-drafting support only and must not directly change Ranking score, AI総合, Investment Score, Forecast values, ranking order, or investment decisions. Advanced export and Execution Gate are shifted to Phase 31 / low priority. Symbol DB background refresh foundation is already implemented, so remaining live-provider wiring is an operational connection task rather than the main next product slice.

Ranking UI readability polish keeps the same layout and logic while shortening candidate-card text, using symbol-first chart labels, compacting the display table to purpose-specific key columns, and separating Decision Report preview from raw Markdown. Ranking Decision Report rows are split into compact candidate memo rows and score-detail rows so Markdown remains readable.

Current UI maturity work also includes Symbol Cockpit polish: the cockpit presents existing analysis results as summary header, KPI cards, price/forecast reading guidance, direction signal cards/details, score breakdown, period evaluation, memo, Research Evidence operation/status + evidence cards, structured Decision Report UI, and lower-priority detail expanders, without changing backend scoring, forecast, Research RAG, or report generation logic.
Cockpit score / forecast / risk wording now makes the existing roles explicit: Investment Score is a comparison / analysis score rather than a trading instruction, Forecast is model-view / uncertainty context rather than a future guarantee, Screening / 基礎評価 is one component of the integrated score, Risk / リスク is a confirmation material rather than a safety guarantee, and Data Quality / データ信頼度 / Research Score remain separate confidence / evidence checks.

Current product maturity work includes global UI tone polish: higher-contrast dark navy financial SaaS theme tokens in `ui/styles.py`, role-based text hierarchy tokens, consistent cards, badges, tables, buttons, AI accent surfaces, raised input / expander / container treatments, compact numeric display, and calmer chart explanations are being applied without changing feature behavior or scoring logic.
SMAI mascot assets now have a canonical reference image at `ui/assets/mascot/smai-mascot-reference.webp`; new mascot variants should preserve that character identity. Current title illustrations cover Cockpit, Ranking, and Rebalance page headers, while Symbol Cockpit keeps the cockpit title art and also uses `smai-mascot-cutout.png` as a transparent `SMAI Copilot` / `SMAI Insight` presence asset. The floating Assistant reuses this cutout mascot with dedicated context motion layers, and should stay contextual to the current page / section rather than becoming a generic static help card.

Symbol Cockpit now treats Price & Forecast as the hero chart: Summary and KPI cards come first, then the price/forecast chart, followed by an `上昇気配` / `下降警戒` section, Score Breakdown, review memo, confirmation summary, Research Evidence cards, structured Decision Report, and Developer / Data Details.

Routine development must keep deterministic `mock` / `csv` behavior available for tests and offline checks while product data fetching defaults to live providers.
The configured default provider is `yahoo` with external providers enabled; Streamlit Cockpit / Ranking / related Market Data selectors show `yahoo` first, while tests use `tests/fixtures/config/local.yaml` to make `mock` explicit and network-free.
Setup now includes Playwright as a dev dependency and installs Chromium for optional Streamlit browser smoke; normal local checks remain network-free and do not require browser automation.
For Research RAG / News RAG, product behavior should prioritize current external sources because freshness is central to the feature. `AI調査を更新` is the standard external-source search action for the selected symbol, currently using an EDINET metadata/link adapter (optional and no-op without `EDINET_API_KEY`), TDnet, company IR site discovery from official website metadata, Google News RSS headline search, and Yahoo Finance as the default composite source set, with external stock-news normalization in place. The Cockpit Research operation card keeps this as the only always-visible action, labels the button area as `調査アクション`, uses `AI調査で確認すること` / `確認方針` before fetch, and uses `企業リサーチレポートを更新しました` plus post-report `追加確認` after fetch to summarize business overview, checked source scope, and next confirmation; lower-frequency news-only refresh, CSV, and detail-toggle controls are not part of the main path. Local documents remain important for deterministic tests, demo seeds, user-saved archives, private notes, and fallback, but they are no longer the intended primary product source. Normal checks still use fake adapters / fixtures and must not depend on live network. Fetched source text should not be retained by default; persistent archive should require a separate explicit user action. External-fetch failure details stay behind `取得失敗の技術詳細`, while the normal warning area explains the fallback in beginner-friendly Japanese.

Current Research Summary maturity work is local-first readability, not external LLM integration. `CompanyResearchSummary` / `CompanyResearchSummaryBuilder` now acts as the primary company-understanding report layer: it first normalizes `CompanyResearchReport`, RAG evidence, provider profile snippets, TDnet / company IR site traces, news, and `ResearchBrief` facts into `CompanyResearchEvidence`, then maps them into `企業リサーチサマリー`, `定量情報サマリー`, `IR情報サマリー`, and `最新ニュース・開示サマリー`. `SecurityResearchTypeDetector` and `ResearchPageViewModelBuilder` now route domestic / foreign individual stocks through the company report path and ETF / fund-like symbols through `ETFResearchSummary`, so ETF screens show fund overview, target assets, benchmark, AUM/NAV, expense ratio, distribution yield, holdings, and ETF-specific checkpoints instead of company IR / earnings-summary prompts; foreign stocks keep the company layout but use Annual Report / 10-K / 10-Q / Earnings Release / SEC Filing wording instead of TDnet / EDINET / 決算短信 prompts. Business profile extraction separates main businesses, supporting businesses, products/services, regions, sector/industry, and customer segments without using news titles as business-profile evidence; when product/service details are absent, it can show conservative `補完候補` labels rather than treating them as official facts. The company overview now leads with what is understood from the external profile, using company name plus main/supporting businesses, products/services, and regions before stating missing segment / profit composition checks. Yahoo Finance provider-profile fields such as market cap, enterprise value, revenue, operating income, net income, EPS, PER/PBR/ROE, dividend yield, and employee count are now mapped into `QuantitativeSummary` when present, including common raw camelCase keys such as `marketCap`, `trailingPE`, `priceToBook`, `returnOnEquity`, `dividendYield`, and `fullTimeEmployees`; `Currency: JPY` context is used to format raw numeric money and EPS values as Japanese yen where possible. JPY / ratio / employee display formatting is applied and missing items are shown only for values that are actually absent. Zero values are kept as valid values, while empty / None / NaN-like values remain missing. Provider raw labels such as `Provider Symbol`, `Quote Type`, `Website`, and internal source names are stripped from user-facing company summaries and fixed-question answers, while profile sector/industry labels are shown as Japanese display labels where mapped. IR情報サマリー now uses rule-based classification v2 with category-specific required / exclude keywords, duplicate source suppression, and classification metadata; generic `tdnet` source type alone stays in 適時開示 and no longer creates 業績予想修正 or 配当・自社株買い rows without explicit keywords such as 業績予想, 上方修正, 配当, or 自己株式取得. The `found` UI status is shown as `関連候補あり` instead of implying PDF-body summarization. TDnet traces can appear as IR rows or latest-topic disclosure rows, using latest-topic `unparsed` when only title/URL-level evidence is available; latest news / disclosure summaries now use an `概要 / 企業理解上の意味 / 追加確認` shape, classify keyword-based topics such as forecast revisions, shareholder returns, business reorganization, products/services, and governance, dedupe repeated TDnet/title rows, and state whether the item is an official disclosure or an external news item requiring official IR confirmation. IR/news/metric rows distinguish `found`, `missing`, `unparsed`, and `unverified`; AI読み取りメモ is folded into a detail expander instead of the initial view. AI調査更新 is optimized without reducing source quality: optional EDINET, TDnet, company IR site, and Yahoo Finance adapters run through the composite adapter in parallel, identical external payload content is not re-registered or re-chunked in the session store, the Streamlit flow uses a short session TTL cache for repeated external fetches, and the rendered summary bundle is reused when the report/news/external-source inputs are unchanged. A display-only `ResearchBrief` / `ResearchMetric` layer still provides detailed AI整理メモ, reading guide, quantitative cards, business overview, confirmation gaps, next actions, and source cards. `InvestmentInsight` / `InvestmentInsightBuilder` and `InvestmentQuestionSummary` / `InvestmentQuestionSummaryBuilder` remain display-only secondary reading aids and do not affect Research Score, Investment Score, or ランキング順位. The first `ResearchFactSummary` slice is implemented as a source-backed fact layer between evidence/news/provider payloads and `ResearchBrief`; it surfaces business overview, key businesses, regions / revenue drivers, checked IR / official disclosures / news, major financial metrics, earnings outlook, shareholder return / dividend policy, recent events, positive materials, caution materials, and missing items as user-readable facts rather than acquisition status. Provider raw fields and English snippets are shaped into Japanese confirmation text for the primary view; the operation card uses pre-fetch `確認方針` and post-report `追加確認`, while source cards, Research Score, source confidence labels, external-source traces, technical fetch-failure details, and raw/source details stay behind expanders when needed. If rule-based shaping becomes insufficient, a local lightweight LLM summarizer can be considered in a later phase while keeping normal checks deterministic and falling back to the rule-based fact summary.

## Repository Layout

- `backend/app`: FastAPI entrypoints and application wiring
- `backend/core`: shared contracts, settings, and domain errors
- `backend/marketdata`: providers, provider registry/factory, live adapter metadata, feature snapshots
- `backend/forecast`: baseline models, model registry lite, evaluation, forecast consensus
- `backend/screening`: explainable symbol screening score
- `backend/scoring`: Investment Score contract/service
- `backend/reporting`: Decision Report context contracts and deterministic Markdown/manifest helpers
- `backend/risk`: deterministic pre-trade risk checks
- `backend/portfolio`: portfolio valuation, no-solver rebalance proposal, portfolio-to-risk workflow
- `ui`: Streamlit application wiring and compatibility imports
- `ui/style_components.py`: state-free display formatting, card HTML, and chart styling helpers
- `ui/ranking_presenter.py`: state-free Ranking display summaries
- `ui/ranking_table.py`: AgGrid-only Ranking table configuration with explicit immutable display config
- `ui/copilot_streaming.py`: deterministic Copilot progressive-text shaping
- `backend/research/normalization.py`: shared Research identifier normalization without service-layer imports
- `backend/research/contracts.py`: Research type aliases, Pydantic contracts, and adapter Protocols
- `ui/copilot_runtime.py`: Copilot Gateway runtime contracts and deterministic status transitions
- `tests`: deterministic regression tests
- `Documents`: requirements, design, roadmap, operation, UI, and work-log documents

## Current Implementation Status

Implemented or mostly implemented:

- Core contracts, settings, YAML loading, and domain errors.
- Product-default `yahoo` live MarketData provider via `yfinance`; single-symbol OHLCV fetches fall back from empty `yf.download` responses to `Ticker.history`, while multi-symbol ranking batches keep the batch result boundary.
- Deterministic `mock` / `csv` MarketData providers for tests, fixtures, and offline checks.
- Provider registry / factory and error mapping for unavailable, timeout, rate-limit, and schema mismatch cases.
- Feature Store Lite style snapshots with last/close, returns, momentum, ADV, volatility, drawdown, missing summary, data quality, and completeness.
- FastAPI endpoints:
  - `GET /health`
  - `POST /risk/pre-trade-check`
  - `POST /portfolio/rebalance-check`
  - `POST /screening/score`
  - `POST /forecast/evaluate`
  - `POST /scoring/investment-score`
- Forecast baseline models: naive, moving average, momentum.
- Forecast consensus / model agreement / forecast range / direction signal / best RMSE model support.
- Screening Score with reason labels and forecast agreement compatibility.
- Investment Score as a separate contract that combines screening, forecast agreement compatibility, direction signal fields, data quality, and risk signal with configurable weights.
- Streamlit left side menu for `銘柄コックピット`, `銘柄ランキング`, `リバランス`, and `設定 / データ情報`.
- Streamlit Market Data provider selector defaults to `yahoo` and shows it first; large live ranking requests warn users instead of hard-blocking them.
- Ranking candidate filters using static/curated metadata before provider fetch.
- Ranking result rows open the selected symbol's local master data in the same symbol-detail modal. The screen now leads with screening summary cards, top-candidate cards, Top 10 score comparison, a condition-aware main chart, and selected-candidate breakdown before the detailed table. The table keeps a short one-line note; the modal carries deeper decision-support guidance from symbol metadata, score components, valuation, income, and next-check points.
- The single-symbol cockpit has a `銘柄データを見る` control beside symbol selection, with date range controls wrapped onto the next row. After data fetch, the cockpit leads with a summary header, KPI cards, price/forecast chart guidance, `上昇気配` / `下降警戒` cards/detail rows, Score Breakdown, period evaluation, investment memo, confirmation summary, Research Evidence operation/status card with evidence cards when fetched, structured Decision Report UI with downloads, and lower-priority detail expanders.
- Symbol universe source import for local curated / JPX expansion; current candidate master carries SBI policy columns but is not yet an SBI-verified tradable universe. SBI acquisition policy is local-master first, not direct site scraping.
- Ranking condition classification first slice: region, product type, ranking purpose, and dynamic detail filters are wired into the Streamlit ranking UI.
- Ranking presets for balanced, upside-signal, data-quality, and lower-risk emphasis.
- Ranking-to-cockpit handoff for follow-up single-symbol review.
- Rebalance Cockpit page with in-page sample/account inputs, percentage target input, allocation comparison chart, result persistence, and beginner-friendly risk breach confirmation points.
- JSON / CSV / Markdown / manifest / ZIP exports for implemented workflows.
- Decision Report context v1 in `backend/reporting` for reusing cockpit / ranking / rebalance outputs in local reports.
- `tools/run_black_check.py` as the routine Black check path for the Windows environment.
- Windows pre-release EXE packaging slice: PyInstaller `onedir` launcher/spec/build script, `README_PRE_RELEASE.txt`, whitelist-based bundled data, and `%LOCALAPPDATA%\SmartMarketAI` runtime cache / outputs / logs / user config.

Partial or intentionally deferred:

- Live provider verification depends on local package, network, and cache/write conditions.
- `polygon` is reserved in provider metadata but adapter implementation is not complete.
- Symbol metadata refresh has a provider-neutral command and an opt-in Yahoo adapter; ranking filters still rely on local symbol-universe metadata before provider fetch.
- SBI Securities ranking-universe policy columns and default exclusion helper are wired into ranking candidate extraction. `tradability=unknown` is allowed for current seed rows; explicit out-of-scope products, not-tradable rows, inactive rows, non-SBI rows, leveraged rows, and inverse rows are excluded.
- Research RAG Phase 20 local evidence slice is implementation-complete as the deterministic foundation. Phase 21 Advanced Research RAG first slices are implemented for query expansion, structured evidence extraction, optional vector / hybrid search contracts, evidence reranking, retrieval quality, grounded answer generation, and the first local `ResearchBriefBuilder` readability layer. Phase 21.5 Stock News RAG has a first local deterministic slice for selected-symbol cockpit news evidence from registered `source_type=news` documents with source URLs; it keeps ランキング順位 unchanged and now serves mainly as test/fallback foundation. Phase 21.6 / 21.7 have a first Cockpit UI slice for optional EDINET securities-report metadata links, TDnet timely-disclosure, company IR site discovery, and Yahoo Finance external profile / news fetch via the default composite adapter. Phase 21.7 also has a backend `ExternalStockNewsAdapter` / `ExternalStockNewsFetchService` slice that normalizes URL-backed external news into `StockNewsEvidence` with viewpoint, sentiment, freshness, dedupe, and network opt-in guard. The EDINET adapter requires `EDINET_API_KEY` for live calls and returns no payloads without it, so normal checks remain network-free. The separate external-permission panel has been removed; `AI調査を更新` is now the standard Cockpit external-source search action and uses the backend safety gate internally. Fetched source text updates the session-local RAG store for the current summary / score / display pass, fetched source trace rows include freshness_status, Cockpit Decision Report includes an external source trace section, and fetched documents / manifests are not retained by default. Phase 22 Research Score has a deterministic service slice, optional Investment Score numeric input with disabled-by-default `research` weight, Cockpit / Ranking Research Summary display rows, selected-candidate breakdown context, and a first Cockpit Decision Report section slice. Research Score のランキング順位統合は意図的に延期し、現在の product path は Ranking で比較し、Cockpit deep dive / Decision Report でユーザーが確認する流れにする。
- Decision Report context/rendering/export is wired into cockpit, ranking, and rebalance UI outputs with Markdown / JSON / manifest / ZIP downloads.
- Roadmap order changed: Phase 23 is now Optional Adapter / advanced analysis. Advanced Forecast Slice 1 has `backend/forecast/adapters/advanced_linear.py`: it provides a lightweight deterministic Ridge-style adapter that predicts 1-60 day forward returns, uses walk-forward / time-series validation instead of random split, returns validation metrics, confidence, feature contribution summary, and keeps default forecast behavior unchanged unless the adapter is selected directly. Slice 2 adds `backend/forecast/advanced_registry.py` and `backend/forecast/adapters/advanced_quantile.py`: `advanced_quantile` uses historical forward-return quantiles to return a center prediction plus lower / upper range fields. Slice 3 adds `backend/forecast/adapters/advanced_tree_sklearn.py`: `advanced_tree_sklearn` uses scikit-learn `ExtraTreesRegressor` by default, supports `RandomForestRegressor` as a constructor option, and returns tree feature importance summaries. Slice 4 adds `backend/forecast/adapters/advanced_gbdt_sklearn.py`: `advanced_gbdt_sklearn` uses scikit-learn `HistGradientBoostingRegressor`, fixed `random_state`, disabled internal early stopping, walk-forward validation, and model-sensitivity feature summaries. Slice 5 adds `AdvancedForecastConsensus` / `summarize_advanced_forecast_evaluations`: registered advanced adapters are combined at one common horizon with capped weights from confidence, error improvement, model agreement, and validation sample context. `POST /forecast/evaluate` now supports `adapter=advanced_linear`, `adapter=advanced_tree_sklearn`, `adapter=advanced_gbdt_sklearn`, and `adapter=advanced_quantile` for 1-60 day advanced forecasts. The Cockpit Streamlit UI makes `AI予測インサイト` the initial forecast focus with actual price, consensus line, prediction range band, conclusion card, and folded advanced / validation / baseline details; Ranking uses the same period-derived single-horizon consensus for auxiliary advanced forecast return / score / confidence table, detail, and CSV context. Ranking now blends advanced forecast upside / downside into direction signals at 25%, and `AI総合` places advanced forecast upside / downside / quality scores inside the tuned `予測・上昇気配30%` and `リスク・下振れ警戒25%` groups with neutral fallback. The low-cost Assistant UI/API work moves to Phase 24 and should resume with the LLM / mascot Assistant implementation. The existing `backend/assistant` deterministic slice remains available: `AssistantRequest` / `AssistantResponse` contracts and `TemplateAssistantService` answer score, risk, research, next-step, and advice-boundary questions from `DecisionReportContext` without LLM or network calls. It returns reasons, cautions, next checkpoints, and cited report sections, and refuses buy/sell/hold-style direction as an explanation boundary rather than investment advice.
- Execution / broker order submission is intentionally out of the current default path.
- UI-rich PDF / Excel report export is future scope after Phase 19; current report exports remain Markdown / JSON / manifest / ZIP.

## Likely Current Phase

- Phase 1〜9: MVP foundation complete.
- Phase 10: External data ingestion path is functionally present; live smoke remains environment-dependent.
- Phase 11〜12: Feature snapshots and Screening Score are implemented.
- Phase 13〜14: Forecast Lab / Multi-Model Forecasting are implementation-complete with deterministic baseline models and consensus.
- Phase 15: Model-Informed Scoring is implementation-complete with `backend/scoring`, API, UI preview/export, and configurable weights.
- Phase 16: Visualization Cockpit / UI improvement is implementation-complete; ranking symbol-detail modal, cockpit symbol-detail button, and investment memo guidance are implemented. Final cross-flow Streamlit browser smoke remains useful before larger backend work.
- Phase 16S: Stabilization smoke has been partially covered through ranking-condition visual checks and targeted UI/helper tests; broader cockpit/ranking/rebalance browser smoke remains optional before handoff.
- Phase 17: UI Polish and ranking-condition redesign is implementation-complete with user visual confirmation.
- Phase 18: Symbol universe metadata refresh is implementation-complete. The completed scope includes CSV schema / enum / decimal / duplicate ticker validation, metadata source/as-of/update timestamps, Settings metadata status, provider-neutral dry-run/manifest refresh, opt-in Yahoo metadata refresh, scoped refresh filters, JPX/SBI/NISA/source import profiles, SBI ranking-universe policy columns/default exclusion, ETF deterministic enrichment, provider-specific `yahoo_symbol` mappings, metadata coverage reporting, and Yahoo dividend-yield scaling fixes. The current candidate master has 9,197 rows: stock 8,087, ETF 1,046, REIT 58, mutual fund 4, ADR 2. The 2026-05-26 refresh added JPX ETF/NISA updates, SBI official US stock/ETF additions, marked 24 SBI-removed symbols as `not_tradable` / `is_sbi_supported=false`, and flagged 16 extreme provider-metric rows as `data_quality=WARN` after targeted Yahoo recheck. MVP ranking/UI remains stock / ETF focused and excludes REIT, leveraged/inverse ETFs, commodity-themed ETFs, non-tradable/non-SBI rows, and mutual-fund seed rows by default. Ongoing NISA / ETF / stock source refreshes, remaining provider/source gaps (`risk_band`, `market_cap_tier`, dividend gaps), and additional live smoke checks are operational maintenance rather than Phase 18 implementation blockers.
- Phase 18 data conversion note: Yahoo dividend-yield scaling now treats JP stock integer percent values as already-percent values rather than multiplying them by 100, fixing over-scaled local master display such as `6857.T`. Tooling mypy issues in symbol-universe import/refresh/enrichment scripts are fixed, so the CI `mypy .` path is expected to pass.
- Phase 19 is complete; Phase 20 local Research RAG evidence work is implementation-complete; Phase 21 Advanced Research RAG / Stock News RAG MVP / local ResearchBrief readability slice and the first Phase 22 Research Score / disabled-by-default Investment Score input / Cockpit Decision Report slices are implemented. Phase 23 Advanced Forecast has backend adapter registry plus `advanced_linear`, `advanced_tree_sklearn`, `advanced_gbdt_sklearn`, `advanced_quantile`, and advanced forecast consensus service / Cockpit / Ranking display slices; Cockpit/API/Ranking use a common 1-60 day forecast horizon across baseline and advanced models for the selected acquisition period. Cockpit default chart / main model cards now focus on advanced forecast consensus and advanced adapters, while simple forecasts remain backend baseline / fallback / detail context. Ranking now blends advanced forecast upside / downside into Ranking direction signals at 25%, and `AI総合` places advanced forecast upside / downside / quality scores inside the tuned `予測・上昇気配30%` and `リスク・下振れ警戒25%` groups with neutral fallback for missing or low-confidence data; ResearchScore ranking-order integration remains deferred. Phase 24 Assistant has a deterministic floating Copilot UI slice for Cockpit / Ranking section-aware Q&A, a dedicated `SMAIアシスタント` workspace with guided question buttons / limited free text / session-local history / chat-width `新しい会話` / live Gateway first slice, Gateway boundary slices (`AssistantContextBundle`, request/response schema, raw-field redaction, `MockAssistantGatewayClient`, opt-in `HttpAssistantGatewayClient`, deterministic fallback service), approved Research Mode `news_fetch` / `research_fetch` connection to the transient `AI調査を更新` external-source path, Decision Report下書き保存/archive UX MVP, and a standalone `smai-ai-gateway/` FastAPI scaffold with provider error detail, `/health/ready`, `/models`, `/api/v1/llm-factor/generate`, model routing, and an opt-in live-smoke path. Phase 24A / 27-A `SMAI LLM Factor` now has deterministic fake/cache/reference display plus the first opt-in live Gateway generation MVP; execution gate and forecast/ranking/scoring integration remain deferred until later validation.
- Phase 19: Decision Report is implementation-complete. It has a local-first backend context contract, deterministic Markdown/manifest/ZIP helpers, standard data-confidence / symbol-metadata / decision-checkpoint sections, and prominent cockpit/ranking/rebalance report blocks with Markdown / JSON / manifest / ZIP downloads plus a collapsed `レポート本文を表示` preview. Cockpit has a collapsible symbol preference filter for the Symbol候補 list, investment-review period presets (`カスタム`, `1週間`, `1か月`, `3か月`, `6か月`, `年初来`, `1年`, `3年`, `5年`) with `カスタム` as the default and help text explaining the review basis for each window. Cockpit also lifts key closed-detail values into a visible confirmation summary and shows period-aware evaluation for the fetched window: return, range position, drawdown, volatility, and short/mid/long review basis. Symbol Cockpit `SMAI Insight` distinguishes upside-dominant, downside-caution, both-high, both-low, model-split, forecast-spread, and data-limited states without advice wording. Ranking reports now emphasize comparison context, score distribution, factor leaders, and group-level deep-dive checkpoints instead of top-symbol metadata sections; ranking report rows include symbol and symbol name, and ranking deep-dive controls render before the lazily generated report so resorting and cockpit handoff stay responsive. Ranking detailed tables use purpose-specific columns, a compact `信頼度/根拠` column only when it adds information, and wrapped confirmation memo to reduce text clipping while retaining long reason/checkpoint details for tooltips and row-click detail. Ranking period presets are `短期: 1か月`, default `標準: 3か月`, `中期: 6か月`, and `長期: 1年`, so direction-signal v2 has enough history for 20日/60日 momentum, volatility, and trend checks. Ranking sort profiles blend market/direction/risk signals with symbol-master `database_fit_score` and `metadata_confidence_score` for dividend, growth, value, stability, and trend purposes. Rebalance builds a Decision Report from holdings, targets, allocation drift, proposed trades, Risk breaches, and confirmation checkpoints.

## Next Good Targets

- Phase 27-A / 27-B live LLM Factor generation MVP is implemented as opt-in: SMAI parent builds compact one-symbol context from Cockpit Research / News / IR evidence, calls `smai-ai-gateway` `/api/v1/llm-factor/generate` when `llm_factor.live.enabled=true`, validates `llm_factor.v1`, caches by context hash / prompt / schema / model / profile, and falls back to deterministic fake/cache on Gateway, timeout, malformed JSON, schema, validation, wrong symbol, unknown evidence, stale source, cache, or provider failure. Cockpit `AI材料分析` explicitly says the result is not reflected in Ranking / Forecast / Investment Score, while LLM runtime metadata stays in closed detail expanders; Ranking, Forecast, AI総合, Investment Score, Research Score, and portfolio/execution behavior are unchanged. Normal tests remain network-free with fake Gateway clients and Playwright panel smoke; real Gateway / Ollama smoke is opt-in via `SMAI_LLM_FACTOR_GATEWAY_LIVE_SMOKE=1`.
- Phase 28-A Cockpit `AI解釈メモ`, Phase 29-A / 29-B Cockpit information architecture cleanup, and Phase 29-C Ranking initial-view / condition-builder compactness are implemented. Ranking now uses a compact `ランキング作成条件` row, side-by-side `選択中の評価方針` / `ランキング条件` summary cards, always-visible attribute / numeric / keyword filters, and a post-condition `ランキング作成` action summary; Ranking score, Forecast, Investment Score, Research Score, LLM Factor, and external-fetch logic are unchanged. Next likely implementation target is Phase 28-B Ranking interpretation, Phase 28-C/D Radar / News interpretation, or confirmation-report draft assistance, while keeping LLM Factor model integration deferred until validation. Phase 26A approved `news_fetch` / `research_fetch` now runs only after user approval, uses fake/local fixtures for normal checks, keeps external fetch transient by default, and can archive Decision Report drafts as sanitized Markdown / ZIP / manifest under `exports/decision_reports/`. The current LLM Factor reference columns and validation reports remain exploratory diagnostics and must not feed Ranking / Forecast / Investment Score weights without later validation and explicit opt-in design.
- Treat NISA / ETF / stock source refreshes, the 11 curated overseas ETF `yahoo_symbol` live-smoke checks, and remaining confirmed-source metadata gaps as operational maintenance after Phase 18 completion.
- Keep a final cross-flow Streamlit smoke available before handoff when browser access is useful: ranking cache/progress, purpose-based resort, ranking symbol-detail modal, cockpit symbol-detail button, cockpit investment memo, and Rebalance wording.
- Phase 20 Research RAG local evidence slice is implementation-complete as the deterministic foundation: `backend/research` local UTF-8 document ingestion, hash dedupe, chunking, freshness-aware keyword evidence search, source-type filters, deterministic Research Summary, data-quality warnings for missing / stale / low-reliability evidence, Settings session-local document upload/registration, explicit cockpit `AI調査を更新` Research Evidence card display, ranking row-click `銘柄データ` modal `AI Research` tab with `AIで資料を確認`, lightweight ranking Research Evidence status display, and Cockpit Decision Report Research Evidence section when documents/evidence exist. Product behavior should now prioritize external fresh sources when AI Research is run; local documents are fixture/archive/fallback rather than the primary live source. Price-data fetch and ranking display do not automatically run full Research RAG analysis for every symbol. Phase 21 Advanced Research RAG has deterministic query expansion, structured evidence extraction, template grounded answers, retrieval quality, evidence reranking, first UI / Decision Report display, optional vector / hybrid retrieval contracts, local embedding generation, optional vector-index build workflow, in-memory local vector store, file-backed vector cache, deterministic hybrid scoring, keyword-fallback hybrid retrieval wrapper, a first Cockpit external fetch slice for optional EDINET securities-report metadata links, TDnet timely disclosures, company IR site discovery, Google News RSS headline search, and Yahoo Finance profile/news with freshness_status trace rows, the first external stock-news adapter/service normalization slice, plus local `ResearchBriefBuilder` display shaping. External live fetch is transient-by-default and does not write fetched documents / manifests in the default UI flow. Phase 22 Research Score has `ResearchScore` / `ResearchScoreService`, optional `research_scores_by_symbol` Investment Score input, config weight `scoring.weights.research` defaulting to 0.0, Cockpit / Ranking Research Summary display rows, report-derived selected-candidate breakdown context, and a Cockpit Decision Report `Research Score` section with component rows, supporting evidence, confidence, warnings, and non-advice notes. ランキング順位と既定スコアは変えず、ResearchScoreのランキング順位統合はユーザーが明示的に再開するまで意図的に延期する。
- Phase 20 seed data includes `data/research_docs/7203_T_yfinance_profile_20260523.md`, fetched from Yahoo Finance via yfinance as a real provider-profile snapshot for local RAG confirmation. Treat it as provider evidence, not an audited filing.
- Long-term Research RAG storage direction: `data/research_docs/` is primarily for deterministic fixtures, demo seeds, user-saved archives, private notes, and fallback. External live sources should not auto-populate it by default; archive/save should be a separate explicit user action with cleanup behavior.
- Project maturity docs are now part of the working baseline: [Documents/96_Manual_UX_Review_Checklist.md](Documents/96_Manual_UX_Review_Checklist.md) tracks manual UX review scenarios, and [Documents/97_Functional_Spec_Issues.md](Documents/97_Functional_Spec_Issues.md) tracks specification ambiguity around Ranking, Cockpit, Rebalance, Decision Report, Research Evidence, Forecast, Risk, and score/confidence hierarchy.
- Decision Report UX / safety review for FS-005 and FS-016 has a first UI wording slice: Cockpit / Ranking / Rebalance report panels now frame reports as analysis memos that save point-in-time materials, evidence, uncertainty, and confirmation points, not buy/sell/hold instructions. Decision Report Markdown includes a format-purpose line, manifest file descriptions explain each artifact, and UI download labels/help distinguish Markdown for reading, JSON for reproduction, manifest for package contents, and ZIP for saving. Phase 21.6 / 21.7 external-source breadth and Research Summary real-screen review have completed their first pass, and 既定のランキング挙動は変更していません。Phase 22 は Cockpit deep-dive / ResearchScore / Decision Report confirmation UX を優先し、Ranking は ResearchScore sorting surface ではなく comparison entry point として扱います。Cockpit ResearchScore UX polish の初期sliceでは、Research Scoreの読み方、要約、観点別内訳、注意点を同じ折りたたみにまとめ、詳細データ側の重複表示を外しました。2026-06-02 のPhase 22回帰では、大阪ガスを含む国内大型/中小型、米国大型/中小型、国内ETF、海外ETF、資料不足ケースの24銘柄で Cockpit Research Summary / Research Score / source 表示を確認し、Ranking AI Research lookup では `Research Score（参考情報）` 文脈が混入なく表示されることを確認しました。Optional EDINET, TDnet, company IR site, Google News RSS, Yahoo Finance, and the first external stock-news adapter/service normalization are wired; explicit save/archive UI, broader Investment News dashboard, Assistant, distribution readiness, and Execution/Broker integration remain planned or future scope unless explicitly assigned.
- Research Summary readability first slice: local rule-based `CompanyResearchSummaryBuilder`, `ResearchBriefBuilder`, display-only `InvestmentInsightBuilder`, and fixed-question `InvestmentQuestionSummaryBuilder` are implemented before any external LLM work. The primary UI order is `企業リサーチサマリー` -> `定量情報サマリー` -> `IR情報サマリー` -> `最新ニュース・開示サマリー` -> `投資ヒントとなるニュース` -> `ニュース・開示の出典を表示（URL付きN件）` -> `企業理解の確認ポイント` -> `詳細情報・開発者向け`. The advanced detail expander omits sections whose purpose overlaps with visible summaries, citations, and confirmation points; it keeps distinct verification data such as Research Score, data-quality warnings, documents/source rows, grounded answers, retrieval quality, extracted claims, evidence detail, and external-source fetch status. News URL display itself already exists in external source traces / detail rows; the Phase 22 news URL UX slice now presents `最新ニュース・開示サマリー` and `投資ヒントとなるニュース` as the visible Market Intelligence card layers, while the source-link expander is folded by default and uses a small citation-style list so it reads as supporting references rather than another summary block. URL-backed news / TDnet / company IR / EDINET / Yahoo Finance entries keep external links with `target="_blank"` / `rel="noopener noreferrer"`, source-specific action labels, no raw URL text in the card body, and a fallback that distinguishes missing news-specific URLs from missing external-source URLs. The current visual treatment uses a restrained neon finance-dashboard style for summary/news cards, but source citations are intentionally muted. The company summary keeps business overview, main businesses, products/services, regions, scale, IR availability, news, missing critical items, and found/missing/unparsed/unverified states explicit. Provider-only information is treated as neutral context. The first metric extraction uses deterministic regex / keyword rules, prefers context-backed unit-bearing metric values for scale wording, and shows missing metrics explicitly. Research Yahoo Finance / company IR adapters reuse the MarketData yfinance cache and shared-session setup for live external-source fetches.
- Cockpit Research Summary now has a display-only `投資ヒントとなるニュース` slice between `最新ニュース・開示サマリー` and the source-link/detail layers. It renders URL-backed `StockNewsEvidence` news only, skips TDnet / provider profile / URL-missing items, and uses a separate `注目材料 Top 3` Market Intelligence panel from IR / disclosure evidence cards. Each card emphasizes title, published date, freshness, source, viewpoint / sentiment chips, short summary, feed-type accent color, right-side link affordance, and a full-card external link; raw URL text and long why-to-check / next-check explanations are kept out of the normal card. Research Score, Investment Score, and ranking order are unchanged. Google News RSS is the first general-news headline breadth adapter; broader paid/API news providers remain planned.
- Phase 22.x Investment News dashboard has the `投資レーダー` screen, the first Standard Mode sourcing layer, and a fuller stock-heatmap-style investment heatmap. `backend/news/dashboard.py` builds deterministic dashboard snapshots from normalized `NewsHeadlineCard` values and provides a network-free demo snapshot; `backend/news/sources.py` adds a bounded Google News RSS dashboard adapter that queries 12 market categories, targets roughly 150-250 raw candidates per manual refresh, dedupes URL/title matches, and stores up to 100 normalized cards. Symbol extraction now keeps text-mentioned symbols first, uses local symbol-universe Japanese aliases / selected short-name patterns for Google News headlines, treats broad topic words such as gold price / defense / index names as `SMAI推測候補` unless a ticker, product name, company name, or explicit code appears, and balances `本文に出た銘柄` with `SMAI推測候補` instead of hiding direct mentions behind a fixed three-item UI cap. Symbol Extraction v2 adds classified `NewsSymbolMatch` evidence with confidence, separates `macro_proxy_symbols` such as TLT / SPY / QQQ / USDJPY / US10Y from clickable cockpit handoff symbols, and avoids showing JPM / 8306.T / REIT ETFs from macro category seeds unless the article has direct or conditional bank / REIT context. Future LLM-gateway symbol rechecks have request / response contracts only; no live LLM call is part of the default news extraction path. The direct-symbol extraction behavior is covered by a 300-case deterministic sprint plus representative v2 macro / bank / Toyota / US market / REIT cases. `ui/views/news.py` renders the `投資レーダー` screen with a mascot title art, a small title-right `情報鮮度` badge that also shows JST acquisition time, an animated market-news headline bar with two-line headline pills, detailed filters for category / freshness / direct vs SMAI inferred symbols / source, Watchlist priority and Watchlist-only display, compact headline cards, a company-name-first clickable heatmap that blends direct news-related symbols with broad local `symbol_universe.csv` candidates scored by category fit, market-cap tier, data quality, news freshness/material, and market signal, three-column category news cards, RAG-style deterministic AI comments / confirmation checkpoints, safe source links, and symbol-name handoff to Symbol Cockpit. Detailed cache size / update-state cards are not shown in the normal top area; refresh failures surface as warnings. Heatmap tile clicks open the selected symbol in the same-app Symbol Cockpit path. It remains out of Investment Score / Research Score / Ranking order; normal checks use fake/static RSS fixtures and do not depend on network.
- Phase 22.y News Background Refresh & Cache Layer is implementation-complete for the backend cache/update foundation. `backend/news` now has strict dashboard snapshot/status contracts, cache normalization limits, latest snapshot save/load, one previous backup, `.tmp` atomic save, cleanup limited to news-dashboard files, cache-size/status helpers, rotating update logs, TTL / minimum-interval skip, bounded retry, and failure fallback to the existing cache. Normal tests remain network-free and cover cache limit, atomic save, cleanup, log rotation, TTL skip, failure fallback, and raw/huge error non-persistence.
- Phase 22.z Symbol Database Background Refresh Layer is implementation-complete as a backend foundation and app-start background integration. `backend/symbols` now has strict refresh contracts, freshness classification, refresh-priority scoring, bounded queue construction, queue/status atomic persistence, in-progress task recovery, stale lock cleanup, latest-only normalized symbol record storage, raw/debug field filtering, one-symbol-at-a-time refresh execution, failure-safe existing-data preservation, and rotating refresh logs. Streamlit startup starts a daemon worker without blocking rendering; the worker refreshes missing/stale symbols from the formal master `data/marketdata/symbol_universe.csv` in a short-session plan of 150 immediately, 75 after 3 minutes, 75 after 8 minutes, then 50 every 5 minutes with fresh-symbol skip and a 1000-symbol per-session cap. Runtime symbol records are stored in `symbols_cache.sqlite`; existing `symbols_cache.json` is treated as a legacy cache import source, not as the primary store or formal master. Symbol record timestamps now distinguish runtime cache time (`cached_at`), source basis (`source_as_of` / `source_updated_at`), and official metric promotion time (`promoted_at`), so CSV-copy timing is not treated as source freshness. Cockpit selected symbols and Ranking comparison targets are registered internally as background priority hints, so workflow-relevant missing / stale records move ahead of ordinary background candidates without adding visible UI controls. Cockpit `データを取得` now prioritizes price / forecast rendering, then registers the selected symbol for background priority refresh with a 30-minute same-symbol session TTL. Ranking `ランキング作成` runs a bounded preflight before ranking creation: comparison candidates up to 30 are refreshed directly, larger requests refresh up to 50 from a 300-symbol scan set, and remaining candidates stay on the background priority path. Ranking preflight immediately promotes only the target symbols into `symbol_metrics.sqlite` when usable official metric fields exist, while background batches continue bulk promotion and prune official metrics for symbols no longer present in `symbol_universe.csv` or marked `is_active=false` when the CSV can be read safely. The runtime symbol cache overlays provider / cache update / source basis / price update / fundamental update / `data_freshness_status` onto `symbol_universe_runtime_rows()`: Cockpit shows a compact selected-symbol `銘柄DB` freshness line, and the shared Ranking/Cockpit `銘柄データ` modal shows `データ情報` rows for freshness, source, update times, and missing key fields. Live provider refresh wiring remains follow-up work.
- 2026-06-04 documentation sync before Phase 22.x clarified the current implementation boundary across README / roadmap / operations / review docs: news cache and symbol DB refresh foundations are implemented, Assistant has backend deterministic service only, and the next product slice was the independent Investment News UI with fake fixture regression.
- Keep provider selection explicit and error messages understandable in UI.
- SMAI always-on server policy now has two recovery layers: the `--resilient` launcher immediately restarts any child Streamlit exit after 2 seconds, including clean exit code 0, while the Windows watcher remains the 5-minute external fallback. Ranking evaluation-policy selectboxes rely on the existing Session State value without also passing a conflicting default index.
- Upward Signal is implemented as an independent third exploration axis beside AI総合 and 上昇気配. `backend/scoring/reversal.py` keeps backward-compatible internal keys while calculating pullback rebound, bottoming, range breakout, accumulation setup, forecast, safety, quality, and mandatory danger caps without changing AI総合 or `total_score`. Ranking uses the public `上向き兆候` policy; Cockpit, Myウォッチリスト snapshots, Ranking History, Assistant context, and Decision Report preserve the score and confirmation wording. It is a deep-review priority for declining, adjusted, or sideways symbols, not a buy signal.
- Phase 30-A Assistant Tool Plan MVP is implemented as a proposal-only layer: `backend/assistant/context_builder.py` builds compact current-page/material context, `tool_registry.py` defines allowed action specs, `tool_plan.py` builds deterministic `AssistantToolPlan`, and `plan_validation.py` rejects unknown / unsafe / unconfirmed external actions. The `SMAIアシスタント` chat response now shows a separate `次にできること` panel with action labels, confirmation status, missing materials, warnings, and a non-advice safety note. No external fetch, ranking creation, report creation, score change, forecast change, or broker action is executed by this Phase 30-A plan display.
- Phase 30-B confirmable navigation first MVP is implemented: navigation Tool Plan steps render same-app links for Ranking / Cockpit / News using the existing `smai_page` query param path, and `cockpit` can open without a symbol. Navigation links do not trigger AI Research refresh, ranking creation, report creation, external fetch, score changes, or forecast changes.
- Phase 30-C Confirmable Safe Actions MVP is implemented for `create_decision_report` and `update_research`: `AssistantActionExecutor`, `AssistantActionResult`, and minimal audit entries wrap user-confirmed action execution, the SMAIアシスタント UI shows an execution confirmation panel and action result card, successful report creation feeds the existing Decision Report draft preview / save flow, and `update_research` uses the existing confirmed AI調査 external-source fetch path while displaying only safe summary fields. `refresh_news` and `create_ranking` remain follow-up actions. No ranking creation, score change, forecast change, AI総合 change, Research Score change, or broker action is performed by this slice.
- Phase 30-D Guided Workflow MVP is implemented as a deterministic guidance layer: `AssistantGuidedWorkflow` represents multi-step `確認フロー` cards for Ranking -> Cockpit -> AI調査 -> 確認レポート, current-Cockpit deep dives, and report-creation intents. Workflow steps can show navigation, review, confirmable-action, manual-check, or blocked states; navigation only opens same-app pages, and `update_research` / `create_decision_report` still require the existing confirmation card before execution. LLM planner, LangGraph-style runtime, automatic price fetch, automatic ranking creation, automatic external fetch, automatic report creation, score / forecast / AI総合 / Research Score changes, and broker actions remain out of scope.
- Phase 30-E LLM Tool Planner MVP is implemented as optional and disabled by default: parent SMAI builds redacted `assistant_tool_plan` requests from current Assistant context and allowed actions, `smai-ai-gateway` exposes generic `/api/v1/assistant/tool-plan`, and parent SMAI adopts only schema-valid, allowlisted, confirmation-safe, non-advice LLM plans into the existing `次にできること` / `確認フロー` UI. Unknown actions, unconfirmed external fetches, unsafe wording, unsupported `create_ranking` / `refresh_news`, Gateway fallback, timeout, and malformed responses are hidden and deterministic Tool Plan / Guided Workflow fallback is used. No scores, forecasts, Ranking, AI総合, Research Score, Decision Report contents, broker/execution behavior, or SMAI/Gateway import boundary changes are made.
- Phase 30-F Agent Evaluation Harness MVP is implemented as a network-free safety regression gate: `backend/assistant/agent_evaluation.py` loads fixture cases, evaluates raw planner responses, adopted planner states, deterministic Tool Plans, and deterministic Guided Workflows, and reports structured pass / fail / warning summaries. Fixtures cover safe workflows, unknown actions, unconfirmed external fetches, unsafe buy/sell/hold and broker/order wording, malformed / timeout fallback, missing Research material handling, and unsupported `create_ranking` ready state. Live LLM quality evaluation remains optional future scope.
- Phase 30-G1 Limited Semi-automatic Workflow Session MVP is implemented in the parent SMAI app: `AssistantWorkflowSession` and `workflow_runtime` hold session-local workflow / step states, only validation-gated Guided Workflows become sessions, existing confirmation cards execute one approved action at a time, action results advance or stop the session, and failed sessions do not fall back to Tool Plan execution prompts. Gateway still proposes JSON only and never runs SMAI actions.
- Phase 30-G2 Workflow Session UI Controls MVP is implemented: active sessions expose current-step skip and workflow cancel controls, failed sessions expose retry / existing-materials recovery / cancel controls, and retry only returns confirmable actions to waiting-confirmation without auto execution.
- Phase 30-H first slice is implemented: a data-driven 16-case Assistant scenario fixture covers smalltalk, self-introduction, concept explanation, SMAI help, Japanese/US stocks, news lookup, data-quality wording, funds/ETFs, ambiguous entities, broad/theme discovery, explicit Ranking/Cockpit navigation, Decision Report creation, and the S&P 500 MAXIM regression. Parent SMAI starts a duplicate-safe background Gateway/model warmup during the Assistant first render, keeps readiness state without blocking Streamlit, shows a restrained loading card with cached Investment Radar headlines or bundled samples, and preserves deterministic fallback for warming/failed/timeout states. Regular checks remain network-free; no Gateway endpoint or parent/child import boundary changed.
- Phase 30-H Assistant loading UI polish is implemented: the loading headline card reuses the Investment Radar mascot art with a CSS radar fallback, warmer startup/fallback copy, source/category labels, and a 2-second Streamlit fragment monitor. A guarded terminal transition performs one full rerun per warmup attempt when status becomes ready / degraded / failed / timeout, then removes the loading panel or leaves a usable fallback panel without clearing text input state, chat history, or workflow session. Static Playwright coverage checks warming icon display, automatic ready transition, fallback input availability, and draft-text preservation.
- Phase 30-H live local UI coverage now includes `tools/playwright_assistant_loading_streamlit_smoke.py`: it starts real Streamlit instances against delayed ready / failed fake Gateway servers and verifies the production loading panel, automatic ready transition, preserved in-progress input, usable failure fallback, and hidden stack traces without external network or Ollama.
- Phase 30-H recovery / dynamic model slice is implemented: warming / retrying / model-loading uses a main-content modal while leaving the Streamlit sidebar above it; startup performs bounded classified retries and then usable fallback / automatic recovery without user-facing reconnect controls. Assistant model selection consumes backward-compatible Gateway `/models` metadata, and chat auto-scroll runs only when message count increases.
- Phase 30-H model selector is implemented as one selectbox beside the chat input. It exposes only models returned by the current environment, orders them by performance, chooses the highest-performance available model by default, and preserves an explicit user choice. Configured model is used only before discovery; fixed model candidates and internal routing profile names are not shown. The selector and chat controls stay fixed at the bottom of the viewport, with redundant model-reason, connection-target, and footer captions removed from below the composer.
- Phase 30-H loading-news readability polish is implemented: cached market headlines are capped at five and rendered as restrained category-colored mini cards with a two-line title clamp and secondary source/acquisition metadata. Broad-market categories receive display priority. Missing cache now produces a non-blocking guidance card instead of demo news or synchronous external retrieval; Playwright covers cached and no-cache loading states.
- Phase 30-H Assistant response policy is implemented: smalltalk / self-introduction / concept explanation suppress Action Cards; broad discovery handles symbol-free theme and sector questions without treating `この相談` as a failed symbol; and Tool Plan / Guided Workflow cards appear only for explicit user requests to navigate, investigate, compare, or create a report. The network-free scenario fixture and Playwright smoke cover the restrained and explicit paths.

## Known Documentation Rules

Ranking History MVP is implemented as a profile-local point-in-time snapshot flow. A successful explicit `ランキング作成` saves a versioned index plus JSON.GZ snapshot under `data/user/profiles/<user_id>/ranking_history/`; the default system profile remains non-persistent. The Ranking page provides history list/detail subviews, pin/unpin, confirmed deletion, 30-item non-pinned retention, five-minute signature dedupe, and safe filter restoration that never auto-runs ranking. History list entries are full-width clickable row cards with alternating/pinned treatments, metadata/condition chips, embedded detail CTA, and top-symbol tags. History detail reuses the live Ranking title, dashboard, condition-card, metric-card, and section-heading styles while using saved rows only for display-only sort choices, top-five cards, a top-ten metric chart, an upside/downside map, deep-dive ordering, and the result table; current Research/LLM enrichment and ranking recalculation remain disabled.

- SMAIアシスタントの current live path uses Gateway by default for chat, and keeps an LLM-first policy even for short greeting / identity / capability / app-help questions. `free_chat` / `identity` / `app_help` / `capability_help` / `screen_guidance` are treated as `llm_micro`: parent SMAI skips tool/RAG/news/symbol-specific context and chat-history payload, sends only minimal SMAIナビ context, and Gateway uses `/no_think`, `think: false`, one quality-regeneration attempt, and natural fallback only after provider / Gateway / validation failure. Notebook runtime is now 25s / 160 tokens for `free_chat` / `identity`, 25s / 220 tokens for `app_help` / `capability_help` / `screen_guidance`, 45s / 600-700 tokens for stock/forecast comparison, 60s / 900 tokens for news/RAG, 75s / 1200-1400 tokens for report drafting/export summary, and 90s / 1200 tokens for LLM factor generation. Missing screen-specific `report_context` no longer bypasses Gateway; SMAI sends a minimal assistant context instead. The UI is organized as header / chat-width `新しい会話` action / material chips / a single chat thread / bottom composer with a 1320px content lane and 1180px chat lane. The model selector, text input, and send button live inside the composer; response actions live inside the assistant message block. Submit now appends an in-thread SMAIナビ pending bubble that displays one current processing step at a time, then replaces that same turn with the final answer through the chat placeholder for normal requests; standalone Streamlit generation spinners are not used for the chat flow.
- Assistant response presentation now sanitizes user-facing answer text, materials, cautions, next checkpoints, copy text, and Markdown memo output before rendering/export. Provider/raw/debug/source-body wording and `privacy_notes` / `safety_notes` / internal implementation notes are filtered out of normal answer cards; Gateway `safety_notes` are no longer merged into user cautions. `llm_micro` intents render as lightweight answer-first bubbles with copy only, while stock/forecast/news/report intents keep denser sections and save/report actions with bounded list lengths.

- Treat code and tests as the source of truth for current behavior.
- Keep `PROJECT_CONTEXT.md` compact; put chronological detail in `Documents/99_Work_Log.md`.
- When implementation changes, update README, roadmap, operation guide, and affected design documents in the same work unit when practical.

## Test And Verification Baseline

Use the project virtual environment when available.

```powershell
.\venv_SMAI\Scripts\python.exe .\tools\run_local_checks.py
.\venv_SMAI\Scripts\python.exe -m pytest tests -q
.\venv_SMAI\Scripts\python.exe -m ruff check . --no-cache
.\venv_SMAI\Scripts\python.exe -m mypy .
.\venv_SMAI\Scripts\python.exe .\tools\run_black_check.py
.\venv_SMAI\Scripts\python.exe -c "from pathlib import Path; [p.read_text(encoding='utf-8') for p in Path('.').rglob('*.md') if '.git' not in p.parts]; print('markdown utf-8 ok')"
```

For UI-affecting work, also confirm the relevant Streamlit screen manually and report what changed from a user's perspective.
