# PROJECT_CONTEXT.md

## Overview

This file is the compact current-state summary for Smart Market AI.
Historical work entries belong in [Documents/99_Work_Log.md](Documents/99_Work_Log.md).

Last updated: 2026-06-11

## Project Summary

Smart Market AI is a Python-based investment support project that combines market-data ingestion, feature generation, screening, deterministic baseline forecasting, model-informed Investment Score, portfolio/risk checks, Streamlit UI, Decision Report context, and Research RAG evidence workflows.

The product direction is to help users compare symbols, inspect provider-backed data, review model signals, understand warnings, and make better-informed investment decisions without turning the app into automated trading.

Current focus is project maturity improvement rather than feature expansion. Functional spec issues and a manual UX review checklist have been introduced so confusing behavior, unclear role boundaries, and investment-advice-like wording can be reviewed before more advanced features are added.

Current UI maturity work includes Ranking visualization polish: existing ranking results are being presented as screening / comparison candidates with summary cards, top-candidate cards, score and confidence charts, selected-candidate breakdown, and a detailed table, without changing backend ranking or scoring logic.

Ranking visualization maturity now uses condition-aware chart profiles, purpose-specific focus summaries, purpose-specific table leading columns, and row-level sorting reasons / checkpoints. The main scatter chart changes by ranking purpose, uses `上昇気配 x 下降警戒` for multi-factor / upside views, falls back to available existing columns, and treats Evaluation Confidence as a data-reliability / coverage support metric except for data-confidence-focused rankings.

Ranking detailed table maturity now keeps beginner comparison columns visible by default: `総合スコア`, `配当利回り`, `PER`, `PBR`, and `ROE`. The Ranking top UI uses `評価方針` as the main path for SMAI composite profiles (`AI総合`, `上昇気配重視`, `モメンタム・トレンド`, `成長クオリティ`, `割安クオリティ`, `高配当の持続性`, `低ボラ・安定`, `リスク調整パフォーマンス`, `小型・成長探索`, `NISA長期適合`, `データ信頼度優先`, `ETF低コスト・コア`, `ETFインカム・分散`). Single-metric sorting is not a top dropdown; users sort displayed candidates locally by clicking detailed-table columns such as `総合スコア`, `配当利回り`, `PER`, `PBR`, `ROE`, `時価総額`, `出来高`, `ボラティリティ`, `Risk`, `データ品質`, `Screening`, `上昇気配`, and `下降警戒`. Missing values remain `N/A` and sort to the end. Ranking refresh is tied to the explicit `最新データを取得して更新` action, while evaluation-policy changes and table sort/search/filter operations reuse fetched data and do not fetch external data.
The Ranking top flow now includes a compact `評価方針・条件・信頼度の読み方` guide before data fetch. It distinguishes evaluation policies from pre-fetch detail filters, and explains that `条件適合度` / `DB信頼度`, NISA, dividend / distribution yield, and ETF cost / income criteria are comparison / coverage checks rather than investment attractiveness, safety, or product-suitability judgments.

Symbol universe ranking metadata was fully refreshed from the explicit opt-in Yahoo provider on 2026-06-01. `data/marketdata/symbol_universe.csv` now has 9,197 / 9,197 rows with `metadata_source=yahoo` and `metadata_as_of=2026-06-01`; remaining provider-missing fields stay blank and display as `N/A`. Dividend yield, PER, PBR, and ROE are guarded at write/display/ranking boundaries so over-scaled or out-of-range values are cleared before they can affect ranking. Web pages remain sanity-check references rather than exact parity requirements because metric definitions and update timing can differ.

Ranking logic now uses `upside_signal_score` and `downside_signal_score` as the primary forecast-derived ranking/cockpit indicators. Direction signal v2 scores forecast edge, model-by-model direction edge, recent price momentum, and simple trend confirmation after volatility adjustment, so future forecast models contribute by direction and magnitude rather than only up/down counts. Agreement / model spread is treated as a confidence adjustment with a 0.70 floor, not as an additive bonus. Ranking / preview signals use fetched feature-history bars rather than only the selected display period, so short ranking periods do not force every symbol into neutral 50 / 50 fallback. `forecast_agreement_score` remains for compatibility and as a supplementary model-consistency metric; older direction net/label fields may remain in backend contracts for compatibility but are not main ranking/cockpit UI indicators.

Phase 23 Advanced Forecast now has an adapter registry plus `advanced_linear`, `advanced_tree_sklearn`, `advanced_gbdt_sklearn`, `advanced_quantile`, and an advanced forecast consensus layer. `scikit-learn==1.5.2` is a runtime dependency for the tree / boosting adapters; `advanced_tree_sklearn` defaults to deterministic `ExtraTreesRegressor`, and `advanced_gbdt_sklearn` uses deterministic `HistGradientBoostingRegressor` with fixed `random_state` and internal early stopping disabled. For Cockpit/API, all four advanced adapters support 1-60 day horizons and Cockpit passes the same period-derived forecast horizon to baseline and advanced models so chart lines, cards, and detail rows compare the same forecast date. The default Cockpit/Ranking horizon is derived from the displayed acquisition period at roughly one twelfth of the window, capped at 60 days. Cockpit charts now lead with actual price, the default `AI予測インサイト` consensus line, and its lower-to-upper prediction range band; advanced model lines and simple forecast lines are added with two grouped chart checkboxes (`高度予測モデル`, `単純予測モデル`) for detail confirmation instead of crowding the initial chart. These checkbox bindings live inside the Altair chart, so they update in the browser without rerunning Streamlit or recalculating forecasts. The chart uses a fixed-color in-chart legend that follows the checked series set and can dim individual displayed series while keeping colors stable. The chart keeps a full `価格チャート` view plus a compact right-side `予測スコープ` chart for the latest actual days and forecast area, and small point markers while keeping the actual-price line thinner. Naive / moving-average / momentum simple forecasts are kept as backend baseline / fallback / detail context but are no longer part of the default Cockpit chart or main model-card display. Cockpit `データを取得` shows a progress bar for input confirmation, price / forecast fetch, score / chart shaping, and display update. Cockpit now shows a conclusion-focused `AI予測インサイト` card before individual advanced model details; the card uses `中心予測` as the main display name for the advanced-model consensus, then shows downside / upside cases with tooltips, forecast price, forecast range, confidence, model agreement, forecast dispersion, reasons, cautions, and forecast horizon in the primary view. Low-confidence or judgement-hold states use an amber accent. Individual advanced model cards are always visible below the chart, while RMSE, error improvement, historical direction accuracy, relatively stable model details, and simple forecast baseline comparisons are folded under `高度予測モデルの詳細を見る`, `検証指標を見る`, and `単純予測との比較を見る`. Forecast chart / card helper text includes beginner-friendly formulas for consensus weighting, baseline models, linear return prediction, tree / boosting split-based checks, and quantile range construction. Ranking rows now retain one common-horizon advanced forecast consensus return (`advanced_forecast_predicted_return`), horizon days, score, and confidence context across table/detail/CSV review when enough local history exists. Ranking detail/breakdown labels now call this `AI予測インサイト`, while table/CSV compatibility columns keep `高度予測` naming. Ranking derives `advanced_forecast_upside_score`, `advanced_forecast_downside_score`, and `advanced_forecast_quality_score` from consensus return, lower range, confidence, agreement, direction accuracy, RMSE improvement, and validation sample context; Ranking rows blend these into `upside_signal_score` / `downside_signal_score` at 25% when present, and `AI総合` additionally uses the derived advanced scores at a modest combined weight. The Ranking UI now shows a `今回のランキング条件` card with the selected evaluation policy, common forecast horizon, grouped `AI総合` weight profile, low-is-better caution for downside metrics, and a folded beginner term table for `AI総合`, `上昇気配`, `下降警戒`, `AI予測インサイト`, advanced upside/downside/quality, and horizon. Low-confidence or missing data is pulled toward neutral 50. Other ranking policies keep their existing profile weights unless explicitly changed later. Advanced forecast outputs remain decision-support context, not investment advice.

Phase 23 closeout now also aligns Ranking and Decision Report wording around `AI予測インサイト`: Ranking sort reasons and checkpoints explain that advanced forecast is blended into `上昇気配` / `下降警戒` up to 25%, selected-candidate breakdown and score detail rows place `AI予測インサイト` next to direction signals, and Cockpit / Ranking Decision Report sections preserve the same advanced-forecast context in score decomposition, candidate memo, score detail, distribution, factor leaders, and group checkpoints.

Phase 24 Assistant now has its first Streamlit UI slice: a fixed floating `SMAI Copilot` mascot opens a contextual question panel without network or LLM calls. Cockpit registers contexts for data setup, `AI予測インサイト`, `上昇気配・下降警戒`, and Decision Report; Ranking registers setup, ranking results, and selected deep-dive candidate contexts, while News / Rebalance / Settings use page-specific fallback contexts. Question answers are pre-rendered from the current section context through the deterministic `TemplateAssistantService`, and question chips switch those answers locally in the browser without query-parameter navigation or Streamlit reruns. The chip switching now uses native `details` / `summary` question controls plus separate answer panels matched by `:has(.smai-floating-assistant-qa-item--N[open])`, so the visible chips stay above the answer text and the selected prepared question remains aligned even when chips wrap to multiple rows. Legacy assistant query parameters only auto-open the panel when they match the current page context, so page navigation does not leave Copilot open on the next screen. Open panels use a local checkbox toggle plus a transparent full-screen backdrop label, so clicking outside the panel closes it in-browser without Streamlit reruns. Answers explain the visible screen, reasons, cautions, and next confirmation points while keeping buy / sell / hold boundaries; page-specific chips now also vary the `見る材料`, `注意点`, and `次に確認すること` lists so switching prepared questions feels like changing the discussion focus, especially for News / Ranking / Rebalance / Settings / Cockpit contexts. The floating trigger now uses the canonical face-up mascot thumbnail with restrained breathing / hover motion, context-aware forecast / ranking classes, bottom-right edge placement, and action-oriented CTA labels such as `予測の読み方を聞く`, `上位理由を聞く`, and `候補の比べ方を聞く`, avoiding artificial face / limb overlays that distort the character at high zoom. LLM基盤はSMAI本体に密結合せず、外部 `LLM Gateway API` 経由で呼び出す方針にする。SMAI側は `AssistantContextBundle`、Gateway client、request/response schema validation、deterministic fallback を持ち、provider routing / prompt tuning / rate limit / model switching はGateway側に寄せる。The Gateway boundary slice is now in `backend/assistant`: `AssistantContextBundle`, `AssistantGatewayRequest`, and `AssistantGatewayResponse` convert `DecisionReportContext` into compact Gateway-safe context while excluding provider raw fields, debug logs, full external source bodies, and source metadata; `AssistantGatewayClient`, network-free `MockAssistantGatewayClient`, and `GatewayBackedAssistantService` prefer valid Gateway responses and fall back to `TemplateAssistantService` on Gateway errors, timeout, schema validation failure, empty answers, or missing context. Request contracts already reserve future chat fields such as `conversation_id`, `message_history`, `active_context_id`, and `referenced_context_ids`. The `smai-ai-gateway/` foundation scaffold is now created inside this repo as a future independent repo / Git submodule candidate: a generic FastAPI AI Gateway with `/health`, `/api/v1/chat`, `/api/v1/summarize`, Pydantic schemas, Ollama client boundary, services, tests, setup docs, prompt policy, provider error detail, opt-in live-smoke test path, and `Project_Specification.md` as the current-specification entry point. It stays generic for SMAI, meeting summaries, AI test tooling, and other apps; it does not move existing SMAI RAG code or add import coupling to SMAI core. The future LLM-backed `SMAI Copilot` chat workspace should be separate from the bottom-right floating quick helper: floating Copilot remains section-local guidance, while the chat workspace carries conversation history and uses the shared `AssistantContextBundle` across Cockpit, Ranking, News, and Decision Report contexts. Limited free-text chat UI, real SMAI-to-Gateway API client wiring, and actual live Gateway smoke execution remain future Phase 24+ work.

Ranking UI readability polish keeps the same layout and logic while shortening candidate-card text, using symbol-first chart labels, compacting the display table to purpose-specific key columns, and separating Decision Report preview from raw Markdown. Ranking Decision Report rows are split into compact candidate memo rows and score-detail rows so Markdown remains readable.

Current UI maturity work also includes Symbol Cockpit polish: the cockpit presents existing analysis results as summary header, KPI cards, price/forecast reading guidance, direction signal cards/details, score breakdown, period evaluation, memo, Research Evidence operation/status + evidence cards, structured Decision Report UI, and lower-priority detail expanders, without changing backend scoring, forecast, Research RAG, or report generation logic.
Cockpit score / forecast / risk wording now makes the existing roles explicit: Investment Score is a comparison / analysis score rather than a trading instruction, Forecast is model-view / uncertainty context rather than a future guarantee, Screening is one component of the integrated score, Risk is a confirmation material rather than a safety guarantee, and Data Quality / Research Score remain separate confidence / evidence checks.

Current product maturity work includes global UI tone polish: higher-contrast dark navy financial SaaS theme tokens in `ui/styles.py`, role-based text hierarchy tokens, consistent cards, badges, tables, buttons, AI accent surfaces, raised input / expander / container treatments, compact numeric display, and calmer chart explanations are being applied without changing feature behavior or scoring logic.
SMAI mascot assets now have a canonical reference image at `ui/assets/mascot/smai-mascot-reference.webp`; new mascot variants should preserve that character identity. Current title illustrations cover Cockpit, Ranking, and Rebalance page headers, while Symbol Cockpit keeps the cockpit title art and also uses `smai-mascot-cutout.png` as a transparent `SMAI Copilot` / `SMAI Insight` presence asset. The floating Assistant reuses this cutout mascot with dedicated context motion layers, and should stay contextual to the current page / section rather than becoming a generic static help card.

Symbol Cockpit now treats Price & Forecast as the hero chart: Summary and KPI cards come first, then the price/forecast chart, followed by an `上昇気配` / `下降警戒` section, Score Breakdown, review memo, confirmation summary, Research Evidence cards, structured Decision Report, and Developer / Data Details.

Routine development must keep deterministic `mock` / `csv` behavior available while allowing explicit opt-in use of live providers such as Yahoo/yfinance.
The configured default provider remains `mock` for local checks and APIs; the Streamlit Market Data provider selector is live-first with `yahoo` as the first/default option and explicit per-fetch opt-in.
For Research RAG / News RAG, product behavior should prioritize current external sources because freshness is central to the feature. `AI調査を更新` is the standard external-source search action for the selected symbol, currently using an EDINET metadata/link adapter (optional and no-op without `EDINET_API_KEY`), TDnet, company IR site discovery from official website metadata, Google News RSS headline search, and Yahoo Finance as the default composite source set, with external stock-news normalization in place. The Cockpit Research operation card keeps this as the only always-visible action, labels the button area as `調査アクション`, uses `AI調査で確認すること` / `確認方針` before fetch, and uses `企業リサーチレポートを更新しました` plus post-report `追加確認` after fetch to summarize business overview, checked source scope, and next confirmation; lower-frequency news-only refresh, CSV, and detail-toggle controls are not part of the main path. Local documents remain important for deterministic tests, demo seeds, user-saved archives, private notes, and fallback, but they are no longer the intended primary product source. Normal checks still use fake adapters / fixtures and must not depend on live network. Fetched source text should not be retained by default; persistent archive should require a separate explicit user action. External-fetch failure details stay behind `取得失敗の技術詳細`, while the normal warning area explains the fallback in beginner-friendly Japanese.

Current Research Summary maturity work is local-first readability, not external LLM integration. `CompanyResearchSummary` / `CompanyResearchSummaryBuilder` now acts as the primary company-understanding report layer: it first normalizes `CompanyResearchReport`, RAG evidence, provider profile snippets, TDnet / company IR site traces, news, and `ResearchBrief` facts into `CompanyResearchEvidence`, then maps them into `企業リサーチサマリー`, `定量情報サマリー`, `IR情報サマリー`, and `最新ニュース・開示サマリー`. `SecurityResearchTypeDetector` and `ResearchPageViewModelBuilder` now route domestic / foreign individual stocks through the company report path and ETF / fund-like symbols through `ETFResearchSummary`, so ETF screens show fund overview, target assets, benchmark, AUM/NAV, expense ratio, distribution yield, holdings, and ETF-specific checkpoints instead of company IR / earnings-summary prompts; foreign stocks keep the company layout but use Annual Report / 10-K / 10-Q / Earnings Release / SEC Filing wording instead of TDnet / EDINET / 決算短信 prompts. Business profile extraction separates main businesses, supporting businesses, products/services, regions, sector/industry, and customer segments without using news titles as business-profile evidence; when product/service details are absent, it can show conservative `補完候補` labels rather than treating them as official facts. The company overview now leads with what is understood from the external profile, using company name plus main/supporting businesses, products/services, and regions before stating missing segment / profit composition checks. Yahoo Finance provider-profile fields such as market cap, enterprise value, revenue, operating income, net income, EPS, PER/PBR/ROE, dividend yield, and employee count are now mapped into `QuantitativeSummary` when present, including common raw camelCase keys such as `marketCap`, `trailingPE`, `priceToBook`, `returnOnEquity`, `dividendYield`, and `fullTimeEmployees`; `Currency: JPY` context is used to format raw numeric money and EPS values as Japanese yen where possible. JPY / ratio / employee display formatting is applied and missing items are shown only for values that are actually absent. Zero values are kept as valid values, while empty / None / NaN-like values remain missing. Provider raw labels such as `Provider Symbol`, `Quote Type`, `Website`, and internal source names are stripped from user-facing company summaries and fixed-question answers, while profile sector/industry labels are shown as Japanese display labels where mapped. TDnet traces can appear both as IR rows and latest-topic disclosure rows, using `unparsed` when only title/URL-level evidence is available; latest news / disclosure summaries now use an `概要 / 企業理解上の意味 / 追加確認` shape, classify keyword-based topics such as forecast revisions, shareholder returns, business reorganization, products/services, and governance, dedupe repeated TDnet/title rows, and state whether the item is an official disclosure or an external news item requiring official IR confirmation. IR/news/metric rows distinguish `found`, `missing`, `unparsed`, and `unverified`; AI読み取りメモ is folded into a detail expander instead of the initial view. AI調査更新 is optimized without reducing source quality: optional EDINET, TDnet, company IR site, and Yahoo Finance adapters run through the composite adapter in parallel, identical external payload content is not re-registered or re-chunked in the session store, the Streamlit flow uses a short session TTL cache for repeated external fetches, and the rendered summary bundle is reused when the report/news/external-source inputs are unchanged. A display-only `ResearchBrief` / `ResearchMetric` layer still provides detailed AI整理メモ, reading guide, quantitative cards, business overview, confirmation gaps, next actions, and source cards. `InvestmentInsight` / `InvestmentInsightBuilder` and `InvestmentQuestionSummary` / `InvestmentQuestionSummaryBuilder` remain display-only secondary reading aids and do not affect Research Score, Investment Score, or ランキング順位. The first `ResearchFactSummary` slice is implemented as a source-backed fact layer between evidence/news/provider payloads and `ResearchBrief`; it surfaces business overview, key businesses, regions / revenue drivers, checked IR / official disclosures / news, major financial metrics, earnings outlook, shareholder return / dividend policy, recent events, positive materials, caution materials, and missing items as user-readable facts rather than acquisition status. Provider raw fields and English snippets are shaped into Japanese confirmation text for the primary view; the operation card uses pre-fetch `確認方針` and post-report `追加確認`, while source cards, Research Score, source confidence labels, external-source traces, technical fetch-failure details, and raw/source details stay behind expanders when needed. If rule-based shaping becomes insufficient, a local lightweight LLM summarizer can be considered in a later phase while keeping normal checks deterministic and falling back to the rule-based fact summary.

## Repository Layout

- `backend/app`: FastAPI entrypoints and application wiring
- `backend/core`: shared contracts, settings, and domain errors
- `backend/marketdata`: providers, provider registry/factory, live adapter metadata, feature snapshots
- `backend/forecast`: baseline models, model registry lite, evaluation, forecast consensus
- `backend/screening`: explainable symbol screening score
- `backend/scoring`: Investment Score contract/service
- `backend/reporting`: Decision Report context contracts and deterministic Markdown/manifest helpers
- `backend/risk`: deterministic pre-trade risk checks
- `backend/portfolio`: portfolio valuation, no-solver rebalance proposal, portfolio-to-risk workflow
- `ui`: Streamlit UI for Market Data / Rebalance workflows
- `tests`: deterministic regression tests
- `Documents`: requirements, design, roadmap, operation, UI, and work-log documents

## Current Implementation Status

Implemented or mostly implemented:

- Core contracts, settings, YAML loading, and domain errors.
- Deterministic `mock` / `csv` MarketData providers.
- Explicit opt-in `yahoo` live provider adapter path via `yfinance`; single-symbol OHLCV fetches fall back from empty `yf.download` responses to `Ticker.history`, while multi-symbol ranking batches keep the batch result boundary.
- Provider registry / factory and error mapping for unavailable, timeout, rate-limit, and schema mismatch cases.
- Feature Store Lite style snapshots with last/close, returns, momentum, ADV, volatility, drawdown, missing summary, data quality, and completeness.
- FastAPI endpoints:
  - `GET /health`
  - `POST /risk/pre-trade-check`
  - `POST /portfolio/rebalance-check`
  - `POST /screening/score`
  - `POST /forecast/evaluate`
  - `POST /scoring/investment-score`
- Forecast baseline models: naive, moving average, momentum.
- Forecast consensus / model agreement / forecast range / direction signal / best RMSE model support.
- Screening Score with reason labels and forecast agreement compatibility.
- Investment Score as a separate contract that combines screening, forecast agreement compatibility, direction signal fields, data quality, and risk signal with configurable weights.
- Streamlit left side menu for `銘柄コックピット`, `銘柄ランキング`, `リバランス`, and `設定 / データ情報`.
- Streamlit Market Data provider selector defaults to `yahoo` and shows it first; large live ranking requests warn users instead of hard-blocking them.
- Ranking candidate filters using static/curated metadata before provider fetch.
- Ranking result rows open the selected symbol's local master data in the same symbol-detail modal. The screen now leads with screening summary cards, top-candidate cards, Top 10 score comparison, a condition-aware main chart, and selected-candidate breakdown before the detailed table. The table keeps a short one-line note; the modal carries deeper decision-support guidance from symbol metadata, score components, valuation, income, and next-check points.
- The single-symbol cockpit has a `銘柄データを見る` control beside symbol selection, with date range controls wrapped onto the next row. After data fetch, the cockpit leads with a summary header, KPI cards, price/forecast chart guidance, `上昇気配` / `下降警戒` cards/detail rows, Score Breakdown, period evaluation, investment memo, confirmation summary, Research Evidence operation/status card with evidence cards when fetched, structured Decision Report UI with downloads, and lower-priority detail expanders.
- Symbol universe source import for local curated / JPX expansion; current candidate master carries SBI policy columns but is not yet an SBI-verified tradable universe. SBI acquisition policy is local-master first, not direct site scraping.
- Ranking condition classification first slice: region, product type, ranking purpose, and dynamic detail filters are wired into the Streamlit ranking UI.
- Ranking presets for balanced, upside-signal, data-quality, and lower-risk emphasis.
- Ranking-to-cockpit handoff for follow-up single-symbol review.
- Rebalance Cockpit page with in-page sample/account inputs, percentage target input, allocation comparison chart, result persistence, and beginner-friendly risk breach confirmation points.
- JSON / CSV / Markdown / manifest / ZIP exports for implemented workflows.
- Decision Report context v1 in `backend/reporting` for reusing cockpit / ranking / rebalance outputs in local reports.
- `tools/run_black_check.py` as the routine Black check path for the Windows environment.
- Windows pre-release EXE packaging slice: PyInstaller `onedir` launcher/spec/build script, `README_PRE_RELEASE.txt`, whitelist-based bundled data, and `%LOCALAPPDATA%\SmartMarketAI` runtime cache / outputs / logs / user config.

Partial or intentionally deferred:

- Live provider verification depends on local package, network, and cache/write conditions.
- `polygon` is reserved in provider metadata but adapter implementation is not complete.
- Symbol metadata refresh has a provider-neutral command and an opt-in Yahoo adapter; ranking filters still rely on local symbol-universe metadata before provider fetch.
- SBI Securities ranking-universe policy columns and default exclusion helper are wired into ranking candidate extraction. `tradability=unknown` is allowed for current seed rows; explicit out-of-scope products, not-tradable rows, inactive rows, non-SBI rows, leveraged rows, and inverse rows are excluded.
- Research RAG Phase 20 local evidence slice is implementation-complete as the deterministic foundation. Phase 21 Advanced Research RAG first slices are implemented for query expansion, structured evidence extraction, optional vector / hybrid search contracts, evidence reranking, retrieval quality, grounded answer generation, and the first local `ResearchBriefBuilder` readability layer. Phase 21.5 Stock News RAG has a first local deterministic slice for selected-symbol cockpit news evidence from registered `source_type=news` documents with source URLs; it keeps ランキング順位 unchanged and now serves mainly as test/fallback foundation. Phase 21.6 / 21.7 have a first Cockpit UI slice for optional EDINET securities-report metadata links, TDnet timely-disclosure, company IR site discovery, and Yahoo Finance external profile / news fetch via the default composite adapter. Phase 21.7 also has a backend `ExternalStockNewsAdapter` / `ExternalStockNewsFetchService` slice that normalizes URL-backed external news into `StockNewsEvidence` with viewpoint, sentiment, freshness, dedupe, and network opt-in guard. The EDINET adapter requires `EDINET_API_KEY` for live calls and returns no payloads without it, so normal checks remain network-free. The separate external-permission panel has been removed; `AI調査を更新` is now the standard Cockpit external-source search action and uses the backend safety gate internally. Fetched source text updates the session-local RAG store for the current summary / score / display pass, fetched source trace rows include freshness_status, Cockpit Decision Report includes an external source trace section, and fetched documents / manifests are not retained by default. Phase 22 Research Score has a deterministic service slice, optional Investment Score numeric input with disabled-by-default `research` weight, Cockpit / Ranking Research Summary display rows, selected-candidate breakdown context, and a first Cockpit Decision Report section slice. Research Score のランキング順位統合は意図的に延期し、現在の product path は Ranking で比較し、Cockpit deep dive / Decision Report でユーザーが確認する流れにする。
- Decision Report context/rendering/export is wired into cockpit, ranking, and rebalance UI outputs with Markdown / JSON / manifest / ZIP downloads.
- Roadmap order changed: Phase 23 is now Optional Adapter / advanced analysis. Advanced Forecast Slice 1 has `backend/forecast/adapters/advanced_linear.py`: it provides a lightweight deterministic Ridge-style adapter that predicts 1-60 day forward returns, uses walk-forward / time-series validation instead of random split, returns validation metrics, confidence, feature contribution summary, and keeps default forecast behavior unchanged unless the adapter is selected directly. Slice 2 adds `backend/forecast/advanced_registry.py` and `backend/forecast/adapters/advanced_quantile.py`: `advanced_quantile` uses historical forward-return quantiles to return a center prediction plus lower / upper range fields. Slice 3 adds `backend/forecast/adapters/advanced_tree_sklearn.py`: `advanced_tree_sklearn` uses scikit-learn `ExtraTreesRegressor` by default, supports `RandomForestRegressor` as a constructor option, and returns tree feature importance summaries. Slice 4 adds `backend/forecast/adapters/advanced_gbdt_sklearn.py`: `advanced_gbdt_sklearn` uses scikit-learn `HistGradientBoostingRegressor`, fixed `random_state`, disabled internal early stopping, walk-forward validation, and model-sensitivity feature summaries. Slice 5 adds `AdvancedForecastConsensus` / `summarize_advanced_forecast_evaluations`: registered advanced adapters are combined at one common horizon with capped weights from confidence, error improvement, model agreement, and validation sample context. `POST /forecast/evaluate` now supports `adapter=advanced_linear`, `adapter=advanced_tree_sklearn`, `adapter=advanced_gbdt_sklearn`, and `adapter=advanced_quantile` for 1-60 day advanced forecasts. The Cockpit Streamlit UI makes `AI予測インサイト` the initial forecast focus with actual price, consensus line, prediction range band, conclusion card, and folded advanced / validation / baseline details; Ranking uses the same period-derived single-horizon consensus for auxiliary advanced forecast return / score / confidence table, detail, and CSV context. Ranking now blends advanced forecast upside / downside into direction signals at 25%, and `AI総合` lightly includes advanced forecast upside / downside / quality scores with neutral fallback. The low-cost Assistant UI/API work moves to Phase 24 and should resume with the LLM / mascot Assistant implementation. The existing `backend/assistant` deterministic slice remains available: `AssistantRequest` / `AssistantResponse` contracts and `TemplateAssistantService` answer score, risk, research, next-step, and advice-boundary questions from `DecisionReportContext` without LLM or network calls. It returns reasons, cautions, next checkpoints, and cited report sections, and refuses buy/sell/hold-style direction as an explanation boundary rather than investment advice.
- Execution / broker order submission is intentionally out of the current default path.
- UI-rich PDF / Excel report export is future scope after Phase 19; current report exports remain Markdown / JSON / manifest / ZIP.

## Likely Current Phase

- Phase 1〜9: MVP foundation complete.
- Phase 10: External data ingestion path is functionally present; live smoke remains environment-dependent.
- Phase 11〜12: Feature snapshots and Screening Score are implemented.
- Phase 13〜14: Forecast Lab / Multi-Model Forecasting are implementation-complete with deterministic baseline models and consensus.
- Phase 15: Model-Informed Scoring is implementation-complete with `backend/scoring`, API, UI preview/export, and configurable weights.
- Phase 16: Visualization Cockpit / UI improvement is implementation-complete; ranking symbol-detail modal, cockpit symbol-detail button, and investment memo guidance are implemented. Final cross-flow Streamlit browser smoke remains useful before larger backend work.
- Phase 16S: Stabilization smoke has been partially covered through ranking-condition visual checks and targeted UI/helper tests; broader cockpit/ranking/rebalance browser smoke remains optional before handoff.
- Phase 17: UI Polish and ranking-condition redesign is implementation-complete with user visual confirmation.
- Phase 18: Symbol universe metadata refresh is implementation-complete. The completed scope includes CSV schema / enum / decimal / duplicate ticker validation, metadata source/as-of/update timestamps, Settings metadata status, provider-neutral dry-run/manifest refresh, opt-in Yahoo metadata refresh, scoped refresh filters, JPX/SBI/NISA/source import profiles, SBI ranking-universe policy columns/default exclusion, ETF deterministic enrichment, provider-specific `yahoo_symbol` mappings, metadata coverage reporting, and Yahoo dividend-yield scaling fixes. The current candidate master has 9,197 rows: stock 8,087, ETF 1,046, REIT 58, mutual fund 4, ADR 2. The 2026-05-26 refresh added JPX ETF/NISA updates, SBI official US stock/ETF additions, marked 24 SBI-removed symbols as `not_tradable` / `is_sbi_supported=false`, and flagged 16 extreme provider-metric rows as `data_quality=WARN` after targeted Yahoo recheck. MVP ranking/UI remains stock / ETF focused and excludes REIT, leveraged/inverse ETFs, commodity-themed ETFs, non-tradable/non-SBI rows, and mutual-fund seed rows by default. Ongoing NISA / ETF / stock source refreshes, remaining provider/source gaps (`risk_band`, `market_cap_tier`, dividend gaps), and additional live smoke checks are operational maintenance rather than Phase 18 implementation blockers.
- Phase 18 data conversion note: Yahoo dividend-yield scaling now treats JP stock integer percent values as already-percent values rather than multiplying them by 100, fixing over-scaled local master display such as `6857.T`. Tooling mypy issues in symbol-universe import/refresh/enrichment scripts are fixed, so the CI `mypy .` path is expected to pass.
- Phase 19 is complete; Phase 20 local Research RAG evidence work is implementation-complete; Phase 21 Advanced Research RAG / Stock News RAG MVP / local ResearchBrief readability slice and the first Phase 22 Research Score / disabled-by-default Investment Score input / Cockpit Decision Report slices are implemented. Phase 23 Advanced Forecast has backend adapter registry plus `advanced_linear`, `advanced_tree_sklearn`, `advanced_gbdt_sklearn`, `advanced_quantile`, and advanced forecast consensus service / Cockpit / Ranking display slices; Cockpit/API/Ranking use a common 1-60 day forecast horizon across baseline and advanced models for the selected acquisition period. Cockpit default chart / main model cards now focus on advanced forecast consensus and advanced adapters, while simple forecasts remain backend baseline / fallback / detail context. Ranking now blends advanced forecast upside / downside into Ranking direction signals at 25%, and `AI総合` lightly includes advanced forecast upside / downside / quality scores with neutral fallback for missing or low-confidence data; ResearchScore ranking-order integration remains deferred. Phase 24 Assistant has a deterministic floating Copilot UI slice for Cockpit / Ranking section-aware Q&A plus Gateway boundary slices (`AssistantContextBundle`, request/response schema, raw-field redaction, `MockAssistantGatewayClient`, deterministic fallback service) and a standalone `smai-ai-gateway/` FastAPI scaffold with provider error detail and an opt-in live-smoke path; LLM-backed `SMAI Copilot` chat workspace, limited free-text UI, real SMAI-to-Gateway client wiring, actual live Gateway smoke execution, and execution gate remain planned.
- Phase 19: Decision Report is implementation-complete. It has a local-first backend context contract, deterministic Markdown/manifest/ZIP helpers, standard data-confidence / symbol-metadata / decision-checkpoint sections, and prominent cockpit/ranking/rebalance report blocks with Markdown / JSON / manifest / ZIP downloads plus a collapsed `レポート本文を表示` preview. Cockpit has a collapsible symbol preference filter for the Symbol候補 list, investment-review period presets (`カスタム`, `1週間`, `1か月`, `3か月`, `6か月`, `年初来`, `1年`, `3年`, `5年`) with `カスタム` as the default and help text explaining the review basis for each window. Cockpit also lifts key closed-detail values into a visible confirmation summary and shows period-aware evaluation for the fetched window: return, range position, drawdown, volatility, and short/mid/long review basis. Symbol Cockpit `SMAI Insight` distinguishes upside-dominant, downside-caution, both-high, both-low, model-split, forecast-spread, and data-limited states without advice wording. Ranking reports now emphasize comparison context, score distribution, factor leaders, and group-level deep-dive checkpoints instead of top-symbol metadata sections; ranking report rows include symbol and symbol name, and ranking deep-dive controls render before the lazily generated report so resorting and cockpit handoff stay responsive. Ranking detailed tables use purpose-specific columns, a compact `信頼度/根拠` column only when it adds information, and wrapped confirmation memo to reduce text clipping while retaining long reason/checkpoint details for tooltips and row-click detail. Ranking period presets are `短期: 1か月`, default `標準: 3か月`, `中期: 6か月`, and `長期: 1年`, so direction-signal v2 has enough history for 20日/60日 momentum, volatility, and trend checks. Ranking sort profiles blend market/direction/risk signals with symbol-master `database_fit_score` and `metadata_confidence_score` for dividend, growth, value, stability, and trend purposes. Rebalance builds a Decision Report from holdings, targets, allocation drift, proposed trades, Risk breaches, and confirmation checkpoints.

## Next Good Targets

- Treat NISA / ETF / stock source refreshes, the 11 curated overseas ETF `yahoo_symbol` live-smoke checks, and remaining confirmed-source metadata gaps as operational maintenance after Phase 18 completion.
- Keep a final cross-flow Streamlit smoke available before handoff when browser access is useful: ranking cache/progress, purpose-based resort, ranking symbol-detail modal, cockpit symbol-detail button, cockpit investment memo, and Rebalance wording.
- Phase 20 Research RAG local evidence slice is implementation-complete as the deterministic foundation: `backend/research` local UTF-8 document ingestion, hash dedupe, chunking, freshness-aware keyword evidence search, source-type filters, deterministic Research Summary, data-quality warnings for missing / stale / low-reliability evidence, Settings session-local document upload/registration, explicit cockpit `AI調査を更新` Research Evidence card display, ranking row-click `銘柄データ` modal `AI Research` tab with `AIで資料を確認`, lightweight ranking Research Evidence status display, and Cockpit Decision Report Research Evidence section when documents/evidence exist. Product behavior should now prioritize external fresh sources when AI Research is run; local documents are fixture/archive/fallback rather than the primary live source. Price-data fetch and ranking display do not automatically run full Research RAG analysis for every symbol. Phase 21 Advanced Research RAG has deterministic query expansion, structured evidence extraction, template grounded answers, retrieval quality, evidence reranking, first UI / Decision Report display, optional vector / hybrid retrieval contracts, local embedding generation, optional vector-index build workflow, in-memory local vector store, file-backed vector cache, deterministic hybrid scoring, keyword-fallback hybrid retrieval wrapper, a first Cockpit external fetch slice for optional EDINET securities-report metadata links, TDnet timely disclosures, company IR site discovery, Google News RSS headline search, and Yahoo Finance profile/news with freshness_status trace rows, the first external stock-news adapter/service normalization slice, plus local `ResearchBriefBuilder` display shaping. External live fetch is transient-by-default and does not write fetched documents / manifests in the default UI flow. Phase 22 Research Score has `ResearchScore` / `ResearchScoreService`, optional `research_scores_by_symbol` Investment Score input, config weight `scoring.weights.research` defaulting to 0.0, Cockpit / Ranking Research Summary display rows, report-derived selected-candidate breakdown context, and a Cockpit Decision Report `Research Score` section with component rows, supporting evidence, confidence, warnings, and non-advice notes. ランキング順位と既定スコアは変えず、ResearchScoreのランキング順位統合はユーザーが明示的に再開するまで意図的に延期する。
- Phase 20 seed data includes `data/research_docs/7203_T_yfinance_profile_20260523.md`, fetched from Yahoo Finance via yfinance as a real provider-profile snapshot for local RAG confirmation. Treat it as provider evidence, not an audited filing.
- Long-term Research RAG storage direction: `data/research_docs/` is primarily for deterministic fixtures, demo seeds, user-saved archives, private notes, and fallback. External live sources should not auto-populate it by default; archive/save should be a separate explicit user action with cleanup behavior.
- Project maturity docs are now part of the working baseline: [Documents/96_Manual_UX_Review_Checklist.md](Documents/96_Manual_UX_Review_Checklist.md) tracks manual UX review scenarios, and [Documents/97_Functional_Spec_Issues.md](Documents/97_Functional_Spec_Issues.md) tracks specification ambiguity around Ranking, Cockpit, Rebalance, Decision Report, Research Evidence, Forecast, Risk, and score/confidence hierarchy.
- Decision Report UX / safety review for FS-005 and FS-016 has a first UI wording slice: Cockpit / Ranking / Rebalance report panels now frame reports as analysis memos that save point-in-time materials, evidence, uncertainty, and confirmation points, not buy/sell/hold instructions. Decision Report Markdown includes a format-purpose line, manifest file descriptions explain each artifact, and UI download labels/help distinguish Markdown for reading, JSON for reproduction, manifest for package contents, and ZIP for saving. Phase 21.6 / 21.7 external-source breadth and Research Summary real-screen review have completed their first pass, and 既定のランキング挙動は変更していません。Phase 22 は Cockpit deep-dive / ResearchScore / Decision Report confirmation UX を優先し、Ranking は ResearchScore sorting surface ではなく comparison entry point として扱います。Cockpit ResearchScore UX polish の初期sliceでは、Research Scoreの読み方、要約、観点別内訳、注意点を同じ折りたたみにまとめ、詳細データ側の重複表示を外しました。2026-06-02 のPhase 22回帰では、大阪ガスを含む国内大型/中小型、米国大型/中小型、国内ETF、海外ETF、資料不足ケースの24銘柄で Cockpit Research Summary / Research Score / source 表示を確認し、Ranking AI Research lookup では `Research Score（参考情報）` 文脈が混入なく表示されることを確認しました。Optional EDINET, TDnet, company IR site, Google News RSS, Yahoo Finance, and the first external stock-news adapter/service normalization are wired; explicit save/archive UI, broader Investment News dashboard, Assistant, distribution readiness, and Execution/Broker integration remain planned or future scope unless explicitly assigned.
- Research Summary readability first slice: local rule-based `CompanyResearchSummaryBuilder`, `ResearchBriefBuilder`, display-only `InvestmentInsightBuilder`, and fixed-question `InvestmentQuestionSummaryBuilder` are implemented before any external LLM work. The primary UI order is `企業リサーチサマリー` -> `定量情報サマリー` -> `IR情報サマリー` -> `最新ニュース・開示サマリー` -> `投資ヒントとなるニュース` -> `ニュース・開示の出典を表示（URL付きN件）` -> `企業理解の確認ポイント` -> `詳細情報・開発者向け`. The advanced detail expander omits sections whose purpose overlaps with visible summaries, citations, and confirmation points; it keeps distinct verification data such as Research Score, data-quality warnings, documents/source rows, grounded answers, retrieval quality, extracted claims, evidence detail, and external-source fetch status. News URL display itself already exists in external source traces / detail rows; the Phase 22 news URL UX slice now presents `最新ニュース・開示サマリー` and `投資ヒントとなるニュース` as the visible Market Intelligence card layers, while the source-link expander is folded by default and uses a small citation-style list so it reads as supporting references rather than another summary block. URL-backed news / TDnet / company IR / EDINET / Yahoo Finance entries keep external links with `target="_blank"` / `rel="noopener noreferrer"`, source-specific action labels, no raw URL text in the card body, and a fallback that distinguishes missing news-specific URLs from missing external-source URLs. The current visual treatment uses a restrained neon finance-dashboard style for summary/news cards, but source citations are intentionally muted. The company summary keeps business overview, main businesses, products/services, regions, scale, IR availability, news, missing critical items, and found/missing/unparsed/unverified states explicit. Provider-only information is treated as neutral context. The first metric extraction uses deterministic regex / keyword rules, prefers context-backed unit-bearing metric values for scale wording, and shows missing metrics explicitly. Research Yahoo Finance / company IR adapters reuse the MarketData yfinance cache and shared-session setup for live external-source fetches.
- Cockpit Research Summary now has a display-only `投資ヒントとなるニュース` slice between `最新ニュース・開示サマリー` and the source-link/detail layers. It renders URL-backed `StockNewsEvidence` news only, skips TDnet / provider profile / URL-missing items, and uses a separate `注目材料 Top 3` Market Intelligence panel from IR / disclosure evidence cards. Each card emphasizes title, published date, freshness, source, viewpoint / sentiment chips, short summary, feed-type accent color, right-side link affordance, and a full-card external link; raw URL text and long why-to-check / next-check explanations are kept out of the normal card. Research Score, Investment Score, and ranking order are unchanged. Google News RSS is the first general-news headline breadth adapter; broader paid/API news providers remain planned.
- Phase 22.x Investment News dashboard has the `投資レーダー` screen, the first Standard Mode sourcing layer, and a fuller stock-heatmap-style investment heatmap. `backend/news/dashboard.py` builds deterministic dashboard snapshots from normalized `NewsHeadlineCard` values and provides a network-free demo snapshot; `backend/news/sources.py` adds a bounded Google News RSS dashboard adapter that queries 12 market categories, targets roughly 150-250 raw candidates per manual refresh, dedupes URL/title matches, and stores up to 100 normalized cards. Symbol extraction now keeps text-mentioned symbols first, uses local symbol-universe Japanese aliases / selected short-name patterns for Google News headlines, treats broad topic words such as gold price / defense / index names as `SMAI推測候補` unless a ticker, product name, company name, or explicit code appears, and balances `本文に出た銘柄` with `SMAI推測候補` instead of hiding direct mentions behind a fixed three-item UI cap. The direct-symbol extraction behavior is covered by a 300-case deterministic sprint. `ui/views/news.py` renders the `投資レーダー` screen with a mascot title art, a small title-right `情報鮮度` badge that also shows JST acquisition time, an animated market-news headline bar with two-line headline pills, detailed filters for category / freshness / direct vs SMAI inferred symbols / source, Watchlist priority and Watchlist-only display, compact headline cards, a company-name-first clickable heatmap that blends direct news-related symbols with broad local `symbol_universe.csv` candidates scored by category fit, market-cap tier, data quality, news freshness/material, and market signal, three-column category news cards, RAG-style deterministic AI comments / confirmation checkpoints, safe source links, and symbol-name handoff to Symbol Cockpit. Detailed cache size / update-state cards are not shown in the normal top area; refresh failures surface as warnings. Heatmap tile clicks open the selected symbol in the same-app Symbol Cockpit path. It remains out of Investment Score / Research Score / Ranking order; normal checks use fake/static RSS fixtures and do not depend on network.
- Phase 22.y News Background Refresh & Cache Layer is implementation-complete for the backend cache/update foundation. `backend/news` now has strict dashboard snapshot/status contracts, cache normalization limits, latest snapshot save/load, one previous backup, `.tmp` atomic save, cleanup limited to news-dashboard files, cache-size/status helpers, rotating update logs, TTL / minimum-interval skip, bounded retry, and failure fallback to the existing cache. Normal tests remain network-free and cover cache limit, atomic save, cleanup, log rotation, TTL skip, failure fallback, and raw/huge error non-persistence.
- Phase 22.z Symbol Database Background Refresh Layer is implementation-complete as a backend foundation and app-start background integration. `backend/symbols` now has strict refresh contracts, freshness classification, refresh-priority scoring, bounded queue construction, queue/status atomic persistence, in-progress task recovery, stale lock cleanup, latest-only normalized symbol record storage, raw/debug field filtering, one-symbol-at-a-time refresh execution, failure-safe existing-data preservation, and rotating refresh logs. Streamlit startup starts a daemon worker without blocking rendering; the worker refreshes missing/stale symbols from the formal master `data/marketdata/symbol_universe.csv` in a short-session plan of 150 immediately, 75 after 3 minutes, 75 after 8 minutes, then 50 every 5 minutes with fresh-symbol skip and a 1000-symbol per-session cap. Runtime symbol records are stored in `symbols_cache.sqlite`; existing `symbols_cache.json` is treated as a legacy cache import source, not as the primary store or formal master. Symbol record timestamps now distinguish runtime cache time (`cached_at`), source basis (`source_as_of` / `source_updated_at`), and official metric promotion time (`promoted_at`), so CSV-copy timing is not treated as source freshness. Cockpit selected symbols and Ranking comparison targets are registered internally as background priority hints, so workflow-relevant missing / stale records move ahead of ordinary background candidates without adding visible UI controls. Cockpit `データを取得` now prioritizes price / forecast rendering, then registers the selected symbol for background priority refresh with a 30-minute same-symbol session TTL. Ranking `最新データを取得して更新` still runs a bounded preflight before ranking creation: comparison candidates up to 30 are refreshed directly, larger requests refresh up to 50 from a 300-symbol scan set, and remaining candidates stay on the background priority path. Ranking preflight immediately promotes only the target symbols into `symbol_metrics.sqlite` when usable official metric fields exist, while background batches continue bulk promotion and prune official metrics for symbols no longer present in `symbol_universe.csv` or marked `is_active=false` when the CSV can be read safely. The runtime symbol cache overlays provider / cache update / source basis / price update / fundamental update / `data_freshness_status` onto `symbol_universe_runtime_rows()`: Cockpit shows a compact selected-symbol `銘柄DB` freshness line, and the shared Ranking/Cockpit `銘柄データ` modal shows `データ情報` rows for freshness, source, update times, and missing key fields. Live provider refresh wiring remains follow-up work.
- 2026-06-04 documentation sync before Phase 22.x clarified the current implementation boundary across README / roadmap / operations / review docs: news cache and symbol DB refresh foundations are implemented, Assistant has backend deterministic service only, and the next product slice was the independent Investment News UI with fake fixture regression.
- Keep provider selection explicit and error messages understandable in UI.

## Known Documentation Rules

- Treat code and tests as the source of truth for current behavior.
- Keep `PROJECT_CONTEXT.md` compact; put chronological detail in `Documents/99_Work_Log.md`.
- When implementation changes, update README, roadmap, operation guide, and affected design documents in the same work unit when practical.

## Test And Verification Baseline

Use the project virtual environment when available.

```powershell
.\venv_SMAI\Scripts\python.exe .\tools\run_local_checks.py
.\venv_SMAI\Scripts\python.exe -m pytest tests -q
.\venv_SMAI\Scripts\python.exe -m ruff check . --no-cache
.\venv_SMAI\Scripts\python.exe -m mypy .
.\venv_SMAI\Scripts\python.exe .\tools\run_black_check.py
.\venv_SMAI\Scripts\python.exe -c "from pathlib import Path; [p.read_text(encoding='utf-8') for p in Path('.').rglob('*.md') if '.git' not in p.parts]; print('markdown utf-8 ok')"
```

For UI-affecting work, also confirm the relevant Streamlit screen manually and report what changed from a user's perspective.
